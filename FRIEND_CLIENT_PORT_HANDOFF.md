# Friend Client Port Handoff

## 2026-09-24 integrated `1.20.1` continuation

- Clean starting HEAD `5e6b34afaa13ae45a9481cffc9a3f720a18c1645`, branch `1.20.1`. Fresh offline Java 25 `compileJava -PportDiagnostics --max-workers=1 --console=plain --offline` reached javac: `build/client-baseline-20260924-turn2.log`, **681 global / 368 exact-owned errors**. The later bloom-guard verification `build/client-bloom-level-guard-20260924.log` also reached javac at **681 / 368** with no `BloomEventListeners` diagnostic. The complete project does not compile; no runtime launch or visual check is possible yet.
- Bounded Luna read-only audits: target pipe and item paths already exist, while old loaders, baked models, and renderer wrappers remain referenced by shared `ClientProxy`, machine builder/item hooks, and generated item models. Do not delete them before requests 10, 12, 18, and 20–22 switch those producers. Neither Luna edited files, ran Gradle, or used Git.
- Sol changed only `client/bloom/BloomEventListeners.java`: chunk unload invalidation now requires the active `Minecraft.level`; level unload cleanup requires a `ClientLevel` and rejects an unrelated currently active client level. The bloom cache is keyed by section position and otherwise a server unload in integrated single-player can clear client geometry at the same coordinates. Patched 26.2 sources confirm the client level type and inclusive max section index. Runtime behavior remains untested.
- Worktree has uncommitted client/docs changes; no reset, stash, pull, merge, commit, push, or PR in this continuation. AI assistance used Codex GPT-6 Sol and GPT-6 Luna. `AI_POLICY.md` human review and editing are pending for these new changes before contribution.

## 2026-09-24 coordinator takeover checkpoint

- Checkout: `C:\Users\georg\IdeaProjects\PatrickStarTech`, branch `1.20.1`, HEAD `a9176b5c9f3b7eb422bdb65767ee0991b99936e8`. It was clean at takeover; current uncommitted changes are confined to owned bloom/GUI/mixin paths and these friend documents. The dedicated `codex/friend-client-port` worktree remains untouched at `ae73bb7c7d85d2b609c6f4e0a1f5195bc10da61c`. Visible Codex tasks using the IDE checkout were idle before editing. No commit, merge, rebase, push, or PR was made.
- Target versions remain Minecraft 26.2, NeoForge 26.2.0.88, Java 25. The patched Minecraft source archive matches the configured NeoForge artifact name. The offline baseline `build/client-takeover-baseline-jdk25-escalated-20260924.log` reached javac: **718 global / 370 exact-owned errors**. An initial wrapper attempt without Java and an attempted sandboxed Java 25 run did not reach javac; they are not baselines. The offline verification after the bloom edit, `build/client-bloom-transition-verify-20260924.log`, also reached javac: **718 global / 370 exact-owned errors**, with no diagnostic in `BloomEventListeners`. Both full builds fail on remaining port errors; `git diff --check` passes.
- Bloom transition correction: `BloomEventListeners#onClientTick` now tracks active-state changes. It invalidates copied section geometry when bloom toggles, and on activation calls the patched 26.2 `LevelRenderer#invalidateCompiledGeometry(ClientLevel, Options, Camera, BlockColors)` using the current level, options, main camera, and block colors. This rebuilds sections that were meshed while bloom was inactive; otherwise they would have no bloom snapshot until an unrelated block update. Exact method and accessors were checked in the patched source archive. Runtime visuals and activation cost remain untested.
- Bloom mixin/reload correction after the audit: `GameRendererAccessor` retains only the verified `resourcePool` field accessor; `BloomShaderManager` counts client ticks locally for its once-per-second availability probe. The owned bloom `LevelRendererMixin` retains the verified `resize(int,int)` hook and drops the removed `graphicsChanged` injection. `BloomEventListeners` reinitializes the post chain on NeoForge `ClientResourceLoadFinishedEvent`, which the patched `Minecraft#onResourceLoadFinished` emits on initial and later successful reloads. Resource reload and active-state changes also clear optional safe-mode builders with the section snapshots. The integrated diagnostic compile reported no errors in these edited paths; runtime shader-reload behavior remains untested.
- Worker and level follow-up: NeoForge's exact `AddSectionGeometryEvent` documentation says the event fires on the main client thread, while its registered section renderer usually runs on a worker and `getLevel()` can return a guidebook level. `BloomChunkGeometry` now rejects non-current levels before using its globally keyed section cache and captures the active model set during the main-thread event, so the worker does not read Minecraft's mutable model manager. Reloading resources while bloom is active also requests a section rebuild; startup omits that extra rebuild. Runtime level-switch and reload visuals remain untested.
- Read-only Luna mixin audit found additional required startup blockers: `ModelBakerImplMixin` targets a removed `bake` overload; `bloom.normal.RenderTypeMixin` targets a removed static chunk-layer list; GUI extractor mixins target old immediate methods/pose; `PlayerInfoAccessor` targets a removed texture map. The shared JSON still lists deleted mixins and omits the valid `StemBlockAccessorMixin`. Requests 24 and 25 capture the new exact actions; requests 3–5, 14, 16, and 23 capture existing JSON changes. Optional third-party target mixins were not validated against their dependency sources.
- GUI follow-up: a Luna implementation migrated `GuiGraphicsMixin` from removed immediate item/tooltip methods to the patched `GuiGraphicsExtractor#item` and `setTooltipForNextFrame`/`setComponentTooltipForNextFrame` overloads. `RenderUtil` preserves research item or fluid substitution with the new method arguments. `ToolChargeBarRenderer` submits its shadow and gradient in ordered extractor strata without the removed flush invoker; the unused `GuiGraphicsAccessor` source was removed after review rather than keeping an empty mixin. The shared `gtceu.mixins.json` still lists this removed accessor and must drop that exact entry before launch. The integrated offline compile `build/client-gui-bloom-integrated-verify-20260924.log` reached javac at **718 global / 370 exact-owned errors**, unchanged from baseline; these edited paths add no diagnostics. GUI tooltip ordering, positioner/style interactions, and rendered bars remain runtime-untested.
- The same integrated compile includes the owned bloom transition, resource-completion hook, worker model-set capture, target resize-only mixin, tick counter, and a null guard in `MultiPlayerGameModeMixin`. No diagnostics occurred in those edited paths. The full project build still fails on legacy/shared 26.2 APIs, so no disposable client launch or visual check ran.
- Remaining exact-owned diagnostics in that log cluster around live legacy loaders/renderers: `FacadeCoverRenderer` (46), `MultiPartBakedModel` (39), `CustomItemRendererWrapperModel` (33), old machine loader/model (54 combined), and old pipe baked/unbaked models (40 combined). These paths still have shared registrations, producers, or resources, so diagnostic count alone is not grounds to delete them. The pinned old Embeddium mixins contribute three diagnostics. This is a diagnostic grouping, not a completion estimate.
- Two Luna audits completed without source edits. Facade legacy classes remain referenced by `ClientProxy`, `GTCovers.FACADE`, `ClientEventListener`, and `models/item/facade_cover.json`; request 17/19 shared changes must select the target routes before retirement. Pipe legacy classes remain referenced by `ClientProxy`, `PipeModelBuilder`, and `PipeModel`/`ActivablePipeModel`; requests 10/21/22 gate the target route. A read-only client mixin audit is active. The ownership table is in `FRIEND_CLIENT_PORT_PLAN.md`.
- The project AI policy requires disclosure of AI-assisted code/text and human understanding, review, and editing before contribution. This checkpoint and code were AI-assisted with Codex (GPT-6 Sol coordinator and GPT-6 Luna agents). Human review has not occurred. No commit, push, or PR was made.
- Next: shared owner removes stale mixin registrations, especially the deleted `client.GuiGraphicsAccessor` and the entries in requests 3–5, 14, 23–24; completes machine/pipe model-data and generated JSON contracts in requests 10, 12, 17, 21–22; and migrates the cape caller in request 25. Then rerun the integrated compile, retire legacy models only after their actual generated resources and registrations switch, and launch a disposable client for bloom/GUI/model visual checks. The global build currently blocks runtime validation.

## Checkout and baseline

- Active IDE checkout: branch `1.20.1` at `C:\Users\georg\IdeaProjects\PatrickStarTech`. Source worktree: `codex/friend-client-port` at `C:\Users\georg\.codex\worktrees\codex-friend-client-port\PatrickStarTech`.
- Current integrated commit: `ae73bb7c7d85d2b609c6f4e0a1f5195bc10da61c`; both checkouts start here. The source worktree’s pre-existing uncommitted model/renderer changes were preserved and 55 changed/new files copied into the clean local `1.20.1` checkout for IDE access. Work since that transfer remains uncommitted in the local checkout. No source worktree files were changed by the transfer, and no commit was created.
- Target: Minecraft 26.2 / NeoForge 26.2.0.88 / Java 25 (checked in version catalog and toolchain).
- Fresh resume command: `.\gradlew.bat compileJava -PportDiagnostics --max-workers=1 --console=plain --offline` (project-configured Java 25).
- Log: `build/friend-client-port-resume-20260924-baseline.log`; 891 global errors and 544 exact-owned-path errors. The command reached javac and failed on the remaining port APIs. Compiler flags and annotation processing were not changed.
- Source evidence archive: `build/moddev/artifacts/minecraft-patched-26.2.0.88-sources.jar`; NeoForge sources from the cached 26.2.0.88 source jar.

- **Newest local integrated compile:** `build/friend-client-port-ctm-wrapped-machine-ber-verify.log`; **718 global / 370 exact-owned diagnostics** (367 assigned client paths, 3 client mixins), unchanged from the preceding local compile. It reached javac and failed on remaining 26.2 port errors. `MachineBlockEntityRenderer` uses the CTM-unwrapping lookup at extraction, submission, visibility, and bounds resolution and adds no diagnostics. The three client-mixin errors are the pinned 1.20.1 Embeddium API integration. No tests or runtime visuals ran.

- **Current integrated offline compile:** `build/friend-client-port-pipe-weighted-ctm-verify.log`; **780 global errors / 432 exact-owned diagnostics** (415 under owned client packages plus 17 under client mixins). It reached javac and failed on remaining legacy model/render and shared APIs; all machine/pipe weighted CTM target files changed in this batch add no diagnostics. The command was `compileJava -PportDiagnostics --max-workers=1 --console=plain --offline` with the configured Java 25 JDK. No tests or runtime visuals ran.

- **Previous integrated offline compile:** `build/friend-client-port-dynamic-snapshot-contract-verify.log`; **780 global errors / 432 exact-owned diagnostics** (415 client renderer/model/util/bloom/particle plus 17 client mixins), down 29 global / exact-owned from the preceding 809/461 pass. This was superseded by the weighted-CTM verification above; no owned-count change resulted.

- Previous verification: `build/friend-client-port-static-cover-parts-verify.log`; **816 global errors / 468 exact-owned diagnostics**. New cover snapshot/model and machine/pipe composition points add no diagnostics.

- Previous verification: `build/friend-client-port-bloom-ticket-submit-verify.log`; **816 global errors / 468 exact-owned diagnostics**. The bloom ticket submitter, target `IRenderSetup`, and `FusionRingRender` are diagnostic-free.

## Work this turn

- Machine BER wrapped-model lookup: the target BER now resolves a machine through `MachineBlockStateModel.find()` at all four model lookup points, so its dynamic descriptor list is still found when a CTM baked wrapper surrounds the machine model; it also checks that the wrapped model belongs to the same machine definition. This follows the existing item special-renderer lookup. Integrated offline compile `build/friend-client-port-ctm-wrapped-machine-ber-verify.log`: **718 global / 370 exact-owned diagnostics**, unchanged from the preceding local pass; the BER class adds no diagnostics. The compile reached javac and failed on remaining project port APIs. No runtime BER check ran.

- Static bloom chunk bridge: added `BloomChunkGeometry` using NeoForge 26.2 `AddSectionGeometryEvent` to retessellate section block models through the target `ModelBlockRenderer`/`BlockStateModelSet` and capture only quads selected by the existing texture metadata/emissivity predicate. Snapshots preserve baked ARGB colors, block tint and shade output, material-emission-adjusted vertex light, packed UVs, overlays, and normals in copied primitive arrays. `SubmitCustomGeometryEvent` submits snapshots only for visible sections through `GTRenderTypes.bloom()`, followed by the existing framegraph post effect at the after-level stage. Build tokens, per-level generations, chunk/level unloads, and resource reload prevent stale model geometry from being retained. The event renderer repeats model tessellation once while a section is rebuilt, so performance and visuals need runtime validation. Vanilla direct chunk-layer mixins/accessor and the obsolete named-type callback were removed; the latter had no in-tree model/resource references, but external users of `gtceu:bloom` remain an unresolved compatibility request (13). Mixin JSON is outside this worktree's ownership; request 23 specifies five obsolete entries to remove before launch. Verification `build/friend-client-port-bloom-section-geometry-verify2.log`: **718 global / 370 exact-owned diagnostics**, down 62 / 62 from 780 / 432. New bloom path files have zero diagnostics. No tests or visual checks ran.

- Weighted CTM candidate migration: inspected patched MC 26.2 `WeightedVariants` and confirmed normal collection chooses one weighted child while the unbaked weighted list is available to the owned codecs. Added generic `CTMWeightedVariants`, recursively baking nested entries and delegating runtime selection, contextual geometry keys, materials, and flags to the target weighted model. Machine and pipe unbaked models use it; `MachineBlockStateModel` and `PipeBlockStateModel` expose every inline child candidate through `CTMModelPartSource`. Integrated offline compile `build/friend-client-port-pipe-weighted-ctm-verify.log`: **780 global / 432 exact-owned diagnostics**, with no diagnostics in the changed machine/pipe/CTM classes. The overall build still fails on legacy/shared 26.2 APIs. `git diff --check` passes; no tests or runtime visuals ran.
- Local IDE checkout and item snapshot follow-up: copied the source worktree’s 55 pre-existing changed/new files into branch `1.20.1`, which shared the same base commit and was clean; left the original source worktree intact and created no commit. Fixed item-snapshot clock calls to use the verified 26.2 `Minecraft#getDeltaTracker()` contract, used `GTUtil#getFluidColor` for both quantum-tank snapshot and legacy tint paths, and imported `GTRenderTypes` for the bloom-ticket invalid sentinel. The local integrated compile fell from 876 to **870 global / 522 owned diagnostics**; no errors remain in the new machine item special renderer/snapshot classes. Added actionable integration request 20 for machine item definitions and `MetaMachineItem#initializeClient`. `git diff --check` passes. Runtime item/facade visuals and tests were not run.
- Pipe model-generator API migration: `PipeModel` and `ActivablePipeModel` now use the repository’s custom block/item model builders and `VariantBlockStateBuilder`, replacing removed Forge generator imports while retaining pipe part geometry and active/inactive state output. `HPCAOverlay` and `WorkableOverlays` also use the actual `ModelFileHelper` type passed by `GTBlockstateProvider`. Verification `build/friend-client-port-pipe-datagen-builder-verify2.log` reports **830 global / 483 exact-owned diagnostics**, down 36 global from the preceding 866-error pass. Two `ActivablePipeModel` errors remain because the unowned `ModelBuilder.FaceBuilder` has no emissivity/AO API; request 21 records the exact required shared contract. The target pipe model codec classes remain clean; generated pipe JSON still needs request 10. `git diff --check` passes; no tests or visuals ran.
- Bloom ticket migration: `BloomHandler` now transfers scheduled tickets into RenderType-keyed lists, checks ticket and world validity, and submits effects during NeoForge 26.2 `SubmitCustomGeometryEvent` with the event collector and camera-relative pose. `IRenderSetup` selects a 26.2 render type rather than mutating a buffer around immediate draws. `BloomRenderer` no longer attempts the removed Tesselator callback path. `FusionRingRender` uses its existing immutable snapshot and dedicated bloom-target pipeline, so its duplicate live-machine ticket callback was removed. Current compile `build/friend-client-port-bloom-ticket-submit-verify.log` reports **816 global / 468 exact-owned errors**; no diagnostics remain in `BloomHandler`, `IRenderSetup`, or `FusionRingRender`. Safe-mode static chunk buffers and named bloom resource routing remain unverified and are not disabled. `git diff --check` passed before compilation; no tests or runtime bloom visuals ran.
- Central monitor snapshot follow-up: `CentralMonitorRender` now captures the slot-0 module output and front/row/column transform into the target dynamic-render state. Text and image modules snapshot their formatted lines or texture quad; text placeholders also snapshot the built-in `rect`/`quad` geometry and provider-backed nested modules. `MonitorGuiRenderer` runs the old hit-test/click forwarding during extraction and emits no geometry, matching its active behavior (its former GUI-texture draw is commented out). 26.2 `BLOCK_INTERACTION_RANGE`, `MouseButtonEvent`, and `TextureTarget` contracts were checked against patched game sources. `build/friend-client-port-monitor-snapshot-verify4.log`: **873 global errors** (534 `client/` path / 17 `core/mixins/client/` path); target snapshot methods add no diagnostics, while legacy `MultiBufferSource` declarations still fail. Custom placeholder renderers still need a provider method in the shared API and must remain behind the BER registration gate until implemented. No tests or monitor visuals ran.
- Machine/facade model composition: `UnbakedMachineBlockStateModel` resolves the facade cover back-plate sprite for the same model generation; each baked `MachineBlockStateModel` now owns a `FacadeBlockStateModel` and combines its contextual parts, geometry key, and material flags with machine geometry. Facade meshes still read only `FACADE_RENDER_STATE` and `COVER_MODEL_DATA` from the supplied `ModelData`; unowned machine-model data production remains required. `build/friend-client-port-machine-facade-composition-verify.log` reports **873 global errors** (534 `client/` path / 17 `core/mixins/client/` path), with no diagnostics in the target machine/facade model classes. Full compile still fails; no tests or runtime facade visuals ran.
- Pipe/facade model composition: `UnbakedPipeBlockStateModel` resolves the cover back-plate sprite; `PipeBlockStateModel` appends facade planes/back plates after pipe/frame parts and includes their snapshot-derived geometry key and material flags. Missing connection-mask fallback still selects center geometry and composes any facade snapshot independently. Verification `build/friend-client-port-machine-pipe-facade-composition-verify.log`: **873 global errors** (534 client-path / 17 client-mixin-path), with no diagnostics in the new pipe or facade target paths. `IPipeNode#getModelData()` still needs to publish copied `FACADE_RENDER_STATE` and per-face child data. No tests or pipe/facade runtime visuals ran.
- Dynamic cover text: `IDynamicCoverRenderer` now exposes a snapshot extraction contract and `CoverTextRenderer` resolves its supplied components into positioned formatted lines before submission. `MachineBlockEntityRenderer` captures each face’s cover payload; `MachineRenderSnapshot` stores only these line snapshots, and deferred submission applies the old face placement/scale and full-bright green text. Verification `build/friend-client-port-dynamic-cover-text-verify.log`: **877 global errors** (538 client-path / 17 client-mixin-path diagnostics); target extraction/submission lines add no diagnostics. The old `MultiBufferSource` callback remains in the compiler set. No tests or visuals ran.

- Ordinary static covers: added immutable `CoverRenderState.capture(ICoverable)` plus `CoverBlockStateModel`, composed into target machine and pipe models with snapshot-derived geometry keys and material flags. Simple covers keep optional `_emissive` lookup; I/O covers keep inverted texture fallback and explicit full-bright emissive quads; generic backplates retain copied plate thickness and occupied-face rules. Facades remain handled by `FacadeBlockStateModel` to avoid duplicate geometry. The 26.2 atlas `DirectoryLister` source contract was checked in the patched game sources; shared model-data publishers and a `block/cover/` atlas directory source are specified in request 22. The latest integrated compile above reports 816/468 and no diagnostics in these target classes. No tests or runtime cover visuals ran.

- Dynamic renderer snapshot conversion: `DynamicRender` now owns target visibility/bounds and extraction/submission without inheriting `IMachineRendererModel`'s removed immediate `MultiBufferSource` callback contract. Assembly-line, fluid-area, fusion-ring, growing-plant, quantum-chest, and quantum-tank target snapshots replace their duplicate immediate paths; `BoilerMultiPartRender` now supplies formed parts only through `ControllerPartModel`. Growing-plant mode functions retain immutable state/offset configuration while target collection applies growth scale/orientation, tint sources, and packed light. Compile `build/friend-client-port-dynamic-snapshot-contract-verify.log`: **780 global / 432 exact-owned diagnostics**, down 29 / 29 from the immediately preceding 809/461 pass. None of these target files add diagnostics. The full compile still reports the old `MachineModel` path and common registrations; no tests or runtime visuals ran.

- Quantum-chest dynamic item migration: `QuantumChestItemRender` now resolves a copied stored/locked stack through `ItemModelResolver.updateForTopItem` into a frame-local `ItemStackRenderState`. Submission preserves fixed display context, item transforms/foil, the animated rotation, captured machine orientation, amount formatting, and creative infinity text. Verification `build/friend-client-port-quantum-chest-item-state-verify.log`: **877 global errors** (538 client-path / 17 client-mixin-path diagnostics); new extraction/submission code is diagnostic-free. The old item and BER callbacks remain, and there was no runtime visual check.

- Fusion ring / dynamic bloom submission: `FusionRingRender` now captures fade alpha/color, ring oscillation, controller-relative axis, and bloom-active state. The target BER submits its normal torus through `GTRenderTypes.lightRing()` and duplicates it to a registered `bloom_light_ring` pipeline whose output target is the current bloom source. `RenderBufferHelper.renderRing` gained a `PoseStack.Pose` overload for collector callbacks. Verification `build/friend-client-port-fusion-ring-submission-verify.log`: **877 global errors** (538 client-path / 17 client-mixin-path diagnostics); target extraction/submission and render-pipeline code add no diagnostics. The old BloomHandler ticket implementation still has removed-API errors and remains until the shared machine renderer is switched; do not run both paths simultaneously. No tests or runtime visuals ran.

- Quantum-tank dynamic fluid migration: `QuantumTankFluidRender` extracts fluid identity, `FluidModel.fluidTintSource().colorAsStack`, amount/capacity, front/up directions, creative state, and gas orientation. Submission emits the textured fluid cube through custom geometry and the amount label through `SubmitNodeCollector.submitText`; `QuantumChestItemRender` now has target-facing orientation/text helpers. `build/friend-client-port-quantum-tank-snapshot-verify2.log` reports **877 global errors** (538 client-path / 17 client-mixin-path diagnostics); target extraction/submission/helper methods have no diagnostics. Legacy immediate and item-stack callbacks remain. No tests or runtime visuals ran.

- Fluid-area dynamic render migration: `FluidAreaRender` now captures the selected fluid, target fluid-model layer, machine-relative faces, immutable offset positions, and per-offset packed light during extraction. The deferred `SubmitNodeCollector` callback only draws from that snapshot; `FluidBlockRenderer` gained a pose-based plane helper that consumes copied lights without retaining/querying a level. `RenderUtil.getFluidLight` now accepts the target `BlockAndLightGetter`, preserving the legacy `BlockAndTintGetter` block helper. Integrated verification `build/friend-client-port-fluid-area-snapshot-verify3.log`: **877 global errors; 538 client-path and 17 client-mixin-path diagnostics**. The target snapshot/submission methods and new light helper have no diagnostics; the full compile still fails on legacy and unrelated APIs. No tests or runtime visuals ran.

- Formed boiler parts: added immutable `ControllerPartRenderState` to `GTModelProperties.FORMED_PART_RENDER_STATE` and the target `ControllerPartModel` contract. `MachineBlockStateModel` resolves the active controller's target model using only copied render-state data and collects retextured child parts; `BoilerMultiPartRender` provides the target firebox/casing selection. Exact shared follow-up is in request 12: `MetaMachine#getModelData()` must publish/clear the controller snapshot and refresh it on formation/state changes. The target classes have no diagnostics in `build/friend-client-port-formed-boiler-parts-verify2.log` (**878 global / 530 exact-owned errors**). The old boiler callback remains. No tests or visuals ran.

- Growing plant submission: `GrowingPlantRender` now extracts configured or recipe-selected plant state, stage/progress, transforms, block tint inputs, and packed lighting into immutable target `BlockModelRenderState` entries; submission uses `SubmitNodeCollector.submitMultiLayer`. Existing selection fallbacks, double-block halves, berries, stem fruit, and scale/offset transforms are retained. The old `MultiBufferSource` callback remains for the legacy caller; its errors are still present in the full compile. Verified in `build/friend-client-port-formed-boiler-parts-verify2.log` (**878 / 530**); no tests or runtime visuals ran.

- Machine BER extraction/submission slice: added `MachineBlockEntityRenderer` implementing patched 26.2 `BlockEntityRenderer<MetaMachine, MachineRenderSnapshot>`, with per-frame block state/definition and indexed dynamic-render payloads. Extraction is the only stage that reads the live machine; submission re-resolves the current model and uses `SubmitNodeCollector`. `DynamicRender` now exposes immutable snapshot hooks, and `AssemblyLineRender` copies progress, relative directions, laser colors, and local line endpoints before deferred geometry submission. The target path stores no live block entity, world, or baked model. `shouldRenderOffScreen()` is renderer-wide in 26.2, so this bridge currently opts in for machine effects and filters per machine in `shouldRender`; verify global-render cost after registration. Compile `build/friend-client-port-machine-ber-assembly-line-verify2.log`: **878 global / 530 exact-owned diagnostics**; the new bridge/snapshot classes have no diagnostics, while the old `MultiBufferSource` callback remains until its legacy caller is retired. Dynamic covers and other dynamic-render descriptors still require snapshots; keep shared registration gated. No tests or runtime visuals ran.

- Earlier machine CTM candidate discovery: added `CTMModelPartSource` to expose baked machine variant/multipart children before a default block state is selected. At the initial `build/friend-client-port-machine-ctm-candidates-verify.log` checkpoint (**878 global / 530 exact-owned diagnostics**), weighted descendants were sampled. The weighted-CTM follow-up above now enumerates machine and pipe descendants recursively while preserving vanilla weighted runtime selection.

- Machine output overlay migration: added immutable `MachineOutputRenderState` and `GTModelProperties.MACHINE_OUTPUT_RENDER_STATE`. The target `MachineBlockStateModel` bakes the legacy pipe/fluid/item overlay sprites as reload-scoped dependencies and collects matching item/fluid output quads, adding the auto-output overlay when enabled. Output state participates in the geometry key and overlay quads contribute material flags. The data snapshot contains only output directions and auto flags, not a live trait or machine. Integrated offline compile `build/friend-client-port-machine-output-overlays-verify.log`: **878 global / 530 exact-owned diagnostics**; target machine model classes have no diagnostics. The full compile remains failed on legacy and unrelated APIs; `git diff --check` passes. Request 12 now specifies the shared producer fields. No tests or runtime visuals ran.

- Facade material-layer follow-up: `FacadeBlockStateModel.materialFlags(level, pos, state)` now includes the flags from the generated backplate quads as well as the facade child models, so animated/translucent backplate content is reflected in target chunk classification. Integrated offline compile `build/friend-client-port-facade-backplate-verify3.log`: **878 global / 530 exact-owned diagnostics**, with no diagnostics in the target facade classes; the full compile remains failed on legacy port APIs. No tests or visuals ran.

- Facade backplate preservation: extended immutable `FacadeRenderState` with the host cover-plate thickness and occupied cover faces. `UnbakedFacadeBlockStateModel` registers `block/cover/cover_back_plate` with `ResolvableModel.Resolver` and bakes its sprite into the reload-scoped target model. `FacadeBlockStateModel` emits the back face and exposed side quads using the old inset bounds, suppressing occupied cull faces and the facade attachment face; geometry keys include the backplate selection. Patched 26.2 `BlockStateModelPart` and `ResolvableModel` sources confirm the part/dependency contracts. Verification `build/friend-client-port-facade-backplate-verify2.log`: **878 global / 530 exact-owned diagnostics**, with no diagnostics in the three target facade classes; full compile remains failed on legacy port APIs. `git diff --check` passes. Request 17 now requires the shared producer to populate both snapshot fields. No tests or visuals ran.

- Machine texture-override dependency follow-up: `UnbakedMachineBlockStateModel.resolveDependencies` now calls `resolver.markDependency` for every `texture_overrides` identifier. The target baker resolves these sprites later through `ModelBaker.materials()`, so registering them during dependency resolution ensures atlas stitching includes the replacement textures. Exact target evidence is patched MC 26.2 `ResolvableModel.Resolver.markDependency(Identifier)`. Integrated offline compile `build/friend-client-port-machine-texture-dependencies-verify.log`: **878 global / 530 exact-owned diagnostics**; target machine, pipe, and facade classes have no diagnostics. `git diff --check` passes. Full compile remains failed on broad port APIs; no tests or runtime visuals ran.

- Machine codec metadata preservation: added `MachineRenderModelOwner`, generalized `DynamicRender`'s parent to that contract, and implemented it on both legacy `MachineModel` and target `MachineBlockStateModel`. The target bake resolves `texture_overrides` to 26.2 block-atlas sprites and carries dynamic render descriptors, replaceable texture keys, and overrides into the baked model. `retextureFormedPartQuads` maps the old `side`/`top`/`bottom`/`all` slots and applies the remapped sprites to target quad lists; overrides remain scoped to formed parts. Integrated offline compile `build/friend-client-port-machine-formed-part-textures-verify.log`: **877 global / 530 exact-owned diagnostics**; target machine/pipe classes add no diagnostics. The legacy machine loader/model still fail on removed Forge APIs; no dynamic BER integration or runtime visuals are claimed. No tests ran.

- Pipe frame and paint slice: `PipeBlockStateModel` resolves `PIPE_FRAME_STATE` against the active 26.2 `BlockStateModelSet`, collects frame parts with their actual `BlockState`, filters covered cull faces using `COVER_MODEL_DATA`, and keys geometry on frame, paint color, and covered sides. For a copied painted color it recolors only child tint layers 0/1 and clears those tint indices; unpainted child and frame colors continue through block tint sources. When connection data is absent it keeps the center-only geometry fallback, with the paint color included in the fallback key. Integrated offline compile `build/friend-client-port-pipe-paint-key-verify.log`: **877 global / 530 exact-owned diagnostics**; both target pipe classes have no diagnostics. Shared snapshot production, actual cover geometry, old loader/generator migration, and visuals remain pending. No tests ran.

- Facade item-model slice: added `FacadeItemModel` as a registered 26.2 `ItemModel` with a resolved base-model dependency, display-transform/particle/light properties, item tint-source layers, foil, and a reload-scoped facade-quad cache. It reads the stack’s facade block state, collects its target block-state parts, preserves north-face filtering, bakes item-context block tint, clips to the inset north plane, and re-interpolates UVs. Verification `build/friend-client-port-facade-item-model-verify2.log`: **878 global / 530 exact-owned diagnostics**, unchanged from the lamp checkpoint; both new facade item classes have zero diagnostics. The new model-data item definition/base asset and legacy loader cleanup remain outside this assignment; request 19 specifies them. Dynamic block-entity/model-data-dependent facade item appearances are not verified. No tests or visuals ran.

- Lamp item special-renderer slice: migrated `LampItemRenderer` to target `SpecialModelRenderer<BlockState>`, added a registered `gtceu:lamp` special-renderer codec, and resolves the lamp’s stack-selected block state into the active 26.2 `BlockStateModelSet`. It submits contextual model parts, block tint-source colors, item foil, and unit-block extents through target render submission. Compile `build/friend-client-port-lamp-special-item-verify.log`: **878 global / 530 exact-owned diagnostics**, down 13 / 14 from the fresh baseline; the new lamp renderer classes have no diagnostics. The old `LampBlockItem#initializeClient` and `GTBlocks` custom-loader asset generation are outside this ownership and remain to be wired; request 18 specifies the exact target model change. No tests or in-game item visuals ran.

- Facade cover model slice: added `FacadeRenderState`, `FacadeBlockStateModel`, the `UnbakedFacadeBlockStateModel` codec, and `FacadeBlockStateModelType#register(RegisterBlockStateModels)`. The model takes facade state plus plate/back-side flags from an immutable snapshot, consumes each side's existing `COVER_MODEL_DATA`, resolves the facade child from the active `BlockStateModelSet`, contextualizes child tint/ModelData at the host position without retaining the host block entity, and clips/interpolates the source quads onto the cover plane. It honors the existing orientation/cull-side rules and block `BlockTintSource` coloring. The legacy item loader and its item display path are not replaced. Compile `build/friend-client-port-facade-model-verify2.log`: **891 global / 544 exact-owned diagnostics**, unchanged; new facade target classes have no diagnostics. No tests or in-game visuals ran. Request 17 records the common model-data and generated machine multipart edits.

- Pipe connection model slice: added `UnbakedPipeBlockStateModel` and `PipeBlockStateModel`, registered the target `gtceu:pipe` codec through `PipeBlockStateModelType#register(RegisterBlockStateModels)`, and added immutable `PIPE_FRAME_STATE`/`PIPE_PAINTING_COLOR` model-data keys for the pending frame/paint producer contract. The target model reads connection and blocked masks through the passed level's model-data snapshot, then selects center, directional connection, and restrictor parts; its geometry key includes both masks and selected child-model keys. It does not look up a live pipe node. Existing active/inactive blockstate variants and separate item-model generation remain separate. Request 10 records the exact outside edits: remove the legacy loader registration in `ClientProxy#onRegisterModelLoaders`, update `PipeModelBuilder#toJson`, and widen the local producer value to `BlockAndTintGetter` without dropping the real level or masks. It also records that frame, cover, and paint parity is still required before generated models switch. Compile `build/friend-client-port-pipe-model-state-verify.log`: **891 global / 544 exact-owned diagnostics**, unchanged; both new target pipe classes have zero diagnostics. `git diff --check` passed. No runtime checks ran.

- Machine block-state model contract: added `FRIEND_CLIENT_ARCHITECTURE.md` before the target model work. Added `GTModelProperties.MACHINE_RENDER_STATE`, target `UnbakedMachineBlockStateModel` codec/dependency resolution, and `MachineBlockStateModel` contextual part collection. The baked model reads only `level.getModelData(pos)`, falls back to the definition default for absent/stale state, preserves weighted target variants and multipart condition ordering, and delegates contextual particle/material flags. NeoForge 26.2.0.88 `DynamicBlockStateModel` and `BlockStateModelExtension` sources document the worker snapshot contract; patched MC 26.2.0.88 `BlockStateModel.Unbaked`, `BlockStateModelPart`, and `BlockModelRenderState` provide the referenced model contracts. Added `MachineBlockStateModelType#register(RegisterBlockStateModels)` for the custom model codec. Exact compile log `build/friend-client-port-machine-state-model-verify3.log`: **891 global / 544 exact-owned diagnostics**, unchanged; the new target model classes report zero diagnostics. This does not yet complete the old machine loader/model replacement, BER extraction/submission, dynamic cover/monitor/bloom path, machine item route, or shared state/data-generation wiring. No tests or runtime visuals ran.

- Growing-plant preview compatibility: `GrowingPlantRender` now uses the target `IntegerProperty.getPossibleValues()` bounds rather than the removed accessor cast, snapshots render offsets as `Vector3fc`, avoids passing the statically typed `Level` where 26.2 requires `BlockAndTintGetter`, and resolves mature `StemBlock` fruit through an owned accessor for the target private registry key. Corrected all configure-only lambdas to the state/progress signature. Integrated offline compile `build/friend-client-port-growing-plant-lambda-fix-verify.log`: **1,584 global / 535 exact-owned diagnostics**, down 6 / 6 from 1,590 / 541. The only remaining diagnostics in this renderer are its legacy `MultiBufferSource` render callback contract; machine-renderer extraction and callback integration remain in request 12. Request 16 asks the shared owner to register `StemBlockAccessorMixin`; shared mixin JSON was not edited. `git diff --check` passes. No tests or preview visuals were run.
- Bloom target-path audit: exact patched 26.2 `ChunkSectionLayer` exposes only the built-in solid/cutout/translucent layers, and NeoForge `AddSectionGeometryEvent` can write additional section geometry only into one of those layers. The patched `LevelRenderer` has no old `renderChunkLayer(RenderType, ...)` entry point targeted by `LevelRendererAccessor`. This confirms the static bloom section-buffer path needs a real target compiled-section/output bridge; do not map bloom content into the normal-world layers or remove it. Request 13 now captures the missing contract. No source changes were made to `BloomHandler` or its registration path in this audit.

- Fusion-ring render-state follow-up: patched 26.2 `DepthStencilState`/`RenderPipeline.Builder` confirm that omitting depth/stencil state preserves the old ring's disabled depth test and depth writes; the ring pipeline already disables culling. `GTRenderTypes.LIGHT_RING_PIPELINE` now explicitly has no depth/stencil state, so `FusionRingRender` no longer mutates global cull/depth state around submission. Replaced the removed frame-delta accessor with `DeltaTracker.getGameTimeDeltaTicks()` so the visual fade advances with game time and remains paused with the game. Integrated offline compile `build/friend-client-port-fusion-ring-game-delta-verify.log`: 1,594 global / 545 exact-owned errors, down 6 / 6; seven legacy `MultiBufferSource`/`BufferBuilder` diagnostics remain in `FusionRingRender` and its bloom callback. `git diff --check` passes (Git reports existing LF-to-CRLF working-copy warnings only). No tests or runtime visuals were run.
- Monitor placeholder NBT follow-up: `QuadPlaceholderRenderer` now unwraps 26.2 `CompoundTag.getFloat`/`getInt` optionals using zero defaults, retaining the old missing-coordinate/color behavior and preserving the four full-bright quad vertices. Exact patched 26.2 `CompoundTag.java` exposes `Optional<Float>` for floats; compiler diagnostics confirmed the integer accessors are optional as well. Integrated offline compile `build/friend-client-port-monitor-quad-nbt-defaults-verify.log`: 1,590 global / 541 exact-owned errors, down 4 / 4. Two diagnostics remain for the unowned API renderer's legacy `MultiBufferSource` contract. `git diff --check` passes; no tests or monitor visuals were run.
- Camera-rotation audit: patched 26.2 `CameraRenderState`/`Camera.extractRenderState` assign pitch to `xRot` and yaw to `yRot`; `EffectRenderContext` reads them in that order and has no diagnostics in the latest compile. Optional Embeddium support remains blocked by the pinned 1.20.1-only artifact; request 15 records the target API/artifact needed. An earlier attempt to remove the legacy `LampItemRenderer` source was rejected by automatic review because it would remove rendering behavior; no source deletion occurred and the rewrite was not retried.

- AoE mining and outline extraction migration: replaced the removed `LevelRenderer.renderLevel`/`renderHitOutline` immediate-draw hooks with target frame-state APIs. `AoEBlockBreakingRenderer` reads the vanilla-extracted hit block's break stage and appends immutable block/state/progress snapshots for the tool's harvestable positions; patched 26.2 `LevelRenderer.submitBlockDestroyAnimation` then handles camera-relative transforms, model parts, and crack submission. `ColoredAoEBlockOutlineRenderer` preserves the material/tier/wire color rules and AoE ordering through `ExtractBlockOutlineRenderStateEvent`, captured shapes and colors, target line collection, and vanilla/high-contrast outline layering. The mixin class was removed because target `LevelRenderer` has no `renderHitOutline`; request 14 asks the integration owner to remove its shared mixin-config entry. Exact patched MC `LevelExtractor`/`LevelRenderer` and NeoForge extraction-event/custom-renderer sources confirm the flow. Integrated offline compile `build/friend-client-port-colored-aoe-outline-verify2.log`: 1,600 global / 551 exact-owned errors, down 3 / 3 from 1,603 / 554; both new renderers have zero diagnostics. `git diff --check` passes. No tests or in-game outlines/mining visuals were run.

- GUI gradient helper migration: replaced `RenderUtil.fillHorizontalGradient`'s removed immediate buffer path with `GuiGraphicsExtractor.fillGradient`, rotating the target top-to-bottom gradient in a scoped `Matrix3x2fStack` so colors still interpolate left-to-right. The old z-depth ordering is expressed with `GuiGraphicsExtractor.nextStratum`; target `GTRenderTypes.guiColor()` was verified to wrap `RenderPipelines.GUI`, the same pipeline used by `fillGradient`. Updated the sole in-tree call in `ToolChargeBarRenderer`. Integrated offline compile `build/friend-client-port-gui-gradient-verify.log`: 1,603 global / 554 exact-owned errors, down 2 / 2 from the freshly rechecked 1,605 / 556 checkpoint; `ToolChargeBarRenderer` has zero diagnostics and the remaining 10 `RenderUtil` diagnostics are its separate old block-model/drawing helper contract. `git diff --check` passes. No GUI runtime visuals or tests were run.

- Bloom post-chain migration: moved the owned bloom graphs and GLSL to the target 26.2 `PostChainConfig`/uniform-buffer contract. `BloomShaderManager` loads the config resources directly, refreshes target UBOs when settings change, and recreates targets/projection state on resize. `BloomRenderer` now imports the main/source/output targets into `FrameGraphBuilder`, adds the post chain, executes against `GameRenderer`'s resource pool, and composites the result back to the main target. The target patched `PostChain`, `PostChainConfig`, `FrameGraphBuilder`, `TextureTarget`, and `GameRenderer` sources confirm these APIs. The first compile exposed removed camera near/far accessors; those are now replaced by near `0.05F` and the existing effective-render-distance far-plane formula. Integrated offline compile `build/friend-client-port-quad-lightmap-audit-verify.log`: 1,605 global / 556 exact-owned diagnostics, down 19 / 19 from the fresh 1,624 / 575 resume checkpoint and 591 / 592 from the original 2,196 / 1,148 baseline. There are zero diagnostics in `BloomShaderManager` or the new framegraph/post-chain code in `BloomRenderer`. Remaining bloom blockers include 45 old safe-mode/chunk-buffer renderer diagnostics, two custom render-stage diagnostics in `BloomHandler`, three removed named-render-type diagnostics, and old bloom mixin hooks. Requests 12–13 document the shared machine-model/BER and named-bloom contracts. `git diff --check` passes; no runtime bloom visuals were run.

- Quad lighting semantics audit: confirmed target 26.2 `BakedQuad` carries packed positions/UVs, colors, normals, and material metadata, but no per-vertex lightmap. `MutableQuadView.fromVanilla` already derived world lighting at submission and did not populate its legacy lightmap scratch slot; `toBakedQuad` had no target field to serialize that scratch. Removed the unconsumed CPU slot/accessors and the ineffective `QuadReInterpolator` lightmap interpolation, preserving target material emission and submission-time world lighting. Repository search found no remaining in-tree lightmap accessor calls. Integrated compile above reports zero diagnostics in `EncodingFormat`, `MutableQuadView`, `QuadView`, and `QuadReInterpolator`; no tests or runtime geometry visuals were run.

- Model reload and selector-cache follow-up: `ModelManagerMixin` now matches the exact 26.2 `ModelManager.reload(SharedState, Executor, PreparationBarrier, Executor)` signature; the removed Forge loading-state check now uses NeoForge 11.0.16's documented `ModLoader.hasErrors()` guard. The exact target `ModelManager` and `ModLoader` sources confirm both contracts. Also replaced `MultiPartBakedModel`'s removed `Util.identityStrategy()` map with `IdentityHashMap`, preserving identity-keyed selector caching. Integrated offline compile `build/friend-client-port-model-manager-multipart-verify.log`: 1,624 global / 575 exact-owned diagnostics, down 1 global / 2 owned from the prior saved checkpoint (572 / 573 from the fresh baseline); `ModelManagerMixin` has zero diagnostics, while the multipart model's larger removed model-API errors remain. `git diff --check` passes. No tests or runtime reload were run.

- World-text helper follow-up: migrated both `RenderBufferHelper.renderInWorldText` overloads from `MultiBufferSource`/immediate `Font.drawInBatch` to the target `SubmitNodeCollector.submitText` path. Camera-relative placement, camera-facing rotation, scale, horizontal centering, see-through display, ARGB color, full-bright lighting, and empty background/outline are retained. The exact patched `OrderedSubmitNodeCollector.submitText` signature confirms the target argument order. Repository search found no in-tree callers of these public helper overloads. Integrated offline compile `build/friend-client-port-render-buffer-text-verify.log`: 1,625 global / 577 exact-owned diagnostics, down 571 / 571 from baseline; `RenderBufferHelper` has zero diagnostics. `git diff --check` passes. No tests or in-world text visuals were run.

- Overheat-particle bloom follow-up: moved `GTOverheatParticle` emissive rendering off the removed `IRenderSetup`/`BufferBuilder` callback into the target `SubmitNodeCollector`. `GTRenderTypes.bloomParticle()` is a dedicated position-color pipeline that writes to the existing bloom target; `GTParticleManager` submits it alongside the unchanged normal pass only while bloom is active. `GTBloomParticle` now exposes the target submission contract instead of registering a legacy bloom ticket. Both passes consume copied pipe-box, color, and alpha snapshots; insulation/frustum visibility, shape expansion, normal particle color/alpha, and separate emissive bloom output remain. The project has one `GTBloomParticle` implementation (`GTOverheatParticle`). Integrated offline compile `build/friend-client-port-overheat-bloom-submit-verify2.log`: 1,628 global / 580 exact-owned diagnostics, down 568 / 568 from baseline; `GTRenderTypes`, `GTParticleManager`, `GTBloomParticle`, and `GTOverheatParticle` have zero diagnostics. `git diff --check` passes. No tests or runtime visuals were run.

- Fluid preview tint follow-up: `FluidBlockRenderer` now reads `FluidModel.fluidTintSource()` from the reloaded 26.2 `FluidStateModelSet` and asks it for the fluid-state tint, using white for a model without a tint source. This preserves the configured ARGB color from `GTClientFluidTypeExtensions.createModel` while replacing the removed `IClientFluidTypeExtensions.getTintColor()` call in all four drawing paths. The patched Minecraft `FluidStateModelSet`/`FluidModel` and cached NeoForge `FluidTintSource` sources confirm the target contract and the project fluid registration uses that same tint source. Integrated offline compile `build/friend-client-port-fluid-block-tint-verify.log`: 1,634 global / 586 exact-owned diagnostics, down 562 / 562 from baseline; `FluidBlockRenderer` has zero diagnostics. `git diff --check` passes. No tests or fluid visuals were run.

- Armor and tank-item rendering follow-up: `ArmorItemModelGenerator` now checks the broken-until-integrated `GTArmorItem`/`GTDyeableArmorItem` types using checked `Class.isInstance`/`Class.cast`, avoiding a javac incompatibility while their common 1.20 `ArmorItem` base is still absent. `GTTankItemFluidPreview` now reads the target `Capabilities.Fluid.ITEM` `ResourceHandler<FluidResource>` through `ItemAccess.forStack`, preserving tank order, fluid amounts, max-icon count, shift gating, and configured toggles. `renderOnTopOfItem` advances the extracted GUI stratum instead of changing global depth state. Exact 26.2 sources confirm the transfer handler contract, `ItemAccess` context, and item-decorator execution after vanilla item decorations. Integrated offline compile `build/friend-client-port-armor-fluid-decorator-verify.log`: 1,639 global / 590 exact-owned errors, down 5 / 5; `HumanoidArmorLayerMixin`, `ArmorItemModelGenerator`, and `GTTankItemFluidPreview` have zero diagnostics. `git diff --check` passes. No tests or runtime visuals were run.

- Armor material tint migration: `HumanoidArmorLayerMixin` now targets 26.2 `EquipmentLayerRenderer.renderLayers` and wraps NeoForge `IClientItemExtensions.getArmorLayerTintColor(ItemStack, Layer, int, int)`. It preserves GTArmorItem's prior channel rule—use the material RGB for white channels and average material RGB with any existing dye/layer tint for non-white channels—and preserves zero as “skip this layer.” The target 26.2 renderer source confirms the exact overload, extension call, color argument, and that rendering submits the tint as ARGB. The old 1.20 `ArmorItem` and `HumanoidArmorLayer.renderModel` assumptions are gone from the owned mixin. Integrated offline compile `build/friend-client-port-armor-tint-verify2.log`: 1,644 global / 595 exact-owned errors, down 8 / 8; this mixin has zero diagnostics. `git diff --check` passes. Runtime mixin application and armor visuals remain untested; `GTArmorItem` itself is outside this assignment and still has target-version errors.

- Follow-up compile cleanup: removed `QuadCornerKicker`'s obsolete no-op `processInPlace(BakedQuad)` override after `QuadTransform` had been migrated to immutable quad results; imported 26.2 NeoForge `FluidStack` in `RenderUtil` for its existing research-fluid preview. The target fluid class is present in the cached NeoForge 26.2.0.88 source jar. Integrated offline compile `build/friend-client-port-quad-fluid-import-verify.log`: 1,652 global / 603 exact-owned errors, down 3 / 3; neither the quad kicker nor the fluid preview references have diagnostics. `git diff --check` passes. No tests or runtime rendering were run.

- Pattern preview migration: `PatternPreviewRenderer` now extracts dirty schema geometry on `ExtractLevelRenderStateEvent` and submits blocks, fluid surfaces, and block entities through `SubmitCustomGeometryEvent`/`SubmitNodeCollector`. Each block uses 26.2 `BlockStateModel.collectParts` and `BlockModelRenderState.submitMultiLayer`, with registered and dynamic tint layers, material translucency, block emission, contextual model data, deterministic variant seeds, state offsets, and the original 0.8 center scale. A `BlockAndTintGetter` adapter reads block/fluid/model-data from the filtered schema, while resolving biome tints and light at the controller's world anchor. Fluids are tessellated by 26.2 `FluidRenderer` into captured immutable per-vertex snapshots grouped by `ChunkSectionLayer`; positions, ARGB tint, UV, overlay, light, and normal are replayed through matching moving-block pipelines. Block entities are extracted into current render-state objects per frame and submitted at controller-relative camera positions; no block entity or schema level is retained by deferred callbacks. Geometry is rebuilt when preview state changes and invalidated by `AddClientReloadListenersEvent`; timeout, cycle-layer filter, active-fluid-sprite tracking, BEs, and fluid rendering remain. Exact target contracts were checked in patched MC `BlockModelRenderState`, `BlockStateModel`, `FluidRenderer`, `BlockEntityRenderDispatcher`, and NeoForge `ExtractLevelRenderStateEvent`/`SubmitCustomGeometryEvent`/`BlockStateModelExtension`. Verification `build/friend-client-port-pattern-preview-submit-verify.log`: 1,655 global / 606 exact-owned errors, down 80 / 80 from the AABB checkpoint; `PatternPreviewRenderer` has zero diagnostics. No runtime visuals or tests were run. Request 9 tracks removal of the remaining shared `ClientEventListener` stage method.

- AABB highlight submission migration: `AABBHighlightRenderer` now snapshots active timed highlights into a `ContextKey` on `LevelRenderState` during `ExtractLevelRenderStateEvent`; submission uses `SubmitCustomGeometryEvent` and the existing no-depth `blockHighlightQuads()` pipeline. `RenderBufferHelper` has a new `PoseStack.Pose` overload that emits the identical 12 thick edges from immutable deferred render data, while the existing `PoseStack` path remains available. Expired entries are removed during synchronized extraction, and blinking timing, camera-relative placement, color, and thickness are preserved. Patched MC 26.2.0.88 `LevelRenderState` and NeoForge 26.2.0.88 extraction/submission event sources verify the state-data and collector contracts. Integrated offline compile `build/friend-client-port-aabb-highlight-submit.log`: 1,735 global / 686 exact-owned diagnostics, down 13 / 13 from the previous checkpoint; no diagnostics remain in `AABBHighlightRenderer`. At that earlier checkpoint `RenderBufferHelper` still had three missing-`MultiBufferSource` diagnostics in its unused public world-text overloads; the follow-up at the top migrates them to the target collector. The AABB overloads already compiled. `git diff --check` passes. Request 8 records removal of the stale shared `ClientEventListener` stage call; the pattern preview call remains until its separate migration.

- Block highlight submission migration: `BlockHighlightRenderer` now uses NeoForge 26.2 `ExtractBlockOutlineRenderStateEvent` and `CustomBlockOutlineRenderer` instead of the removed `RenderHighlightEvent.Block`/`MultiBufferSource` direct draw path. Tool-grid, cover-placement, and pipe-face selection still produce the same four pulsing inset edges and oriented, margined face icons; the callback submits lines plus see-through textured quads with full-bright light, while allowing vanilla's outline to remain additive. Extraction snapshots texture IDs, transformed coordinates, UVs, colors, and the pulse value; the deferred callback retains no level, player, or block entity. Exact contracts were checked in NeoForge `ExtractBlockOutlineRenderStateEvent`/`CustomBlockOutlineRenderer` and patched MC `LevelExtractor`/`LevelRenderer`. Integrated offline compile `build/friend-client-port-block-highlight-submit.log`: 1,748 global / 699 owned, down 18 / 18 from the previous 1,766 / 717 checkpoint, with zero diagnostics in `BlockHighlightRenderer`; `git diff --check` passes. Request 7 records the required removal of the now-obsolete shared `ClientEventListener` call.

- Quad mesh contract follow-up: migrated the owned quad bridge from Forge's removed `IQuadTransformer`/integer-array vertex layout to immutable 26.2 `BakedQuad` records and an explicitly private CPU mesh layout. `fromVanilla`/`toBakedQuad` retain target packed UVs, ARGB colors, normals, material layer/item render type/tint/shade/AO/emission, cull/light/normal flags, and the GT texture key; mesh metadata now travels alongside vertices instead of being discarded. The CPU stride documents that its compatibility lightmap slot is not the 26.2 quad/GPU layout. `QuadTransform` now returns a copied immutable quad or filters it, with no in-repository legacy `processInPlace` consumer. Patched MC `BakedQuad`/`MaterialInfo` and NeoForge `BakedColors`/`BakedNormals` sources verify the contracts. Integrated offline compile `build/friend-client-port-quad-mesh-contract-verify.log`: 1,766 global / 717 owned diagnostics, zero in `EncodingFormat`, `Mesh`, `MeshBuilder`, `MutableQuadView`, `QuadView`, and `QuadTransform`; `git diff --check` passes. Follow-up semantic audit remains for the old per-vertex lightmap interpolation scratch: target quads no longer carry per-vertex lightmaps, so ensure this cannot misleadingly affect behavior.

- Bloom validator/config slice immediately before the mesh migration: `BloomShaderManager` now uses Configuration 4.1.3+26.2's `Validator<BloomType>`/`ValidationResult` and 26.2 `ClickEvent.OpenFile(Path)`. Removed only unused legacy `ShaderInstance` registrations/getters after confirming there were no consumers; the bloom pipeline shader IDs remain registered in `GTRenderTypes`. Exact cached Configuration bytecode and patched MC click-event source confirmed the APIs. `build/friend-client-port-bloom-config-verify.log`: 1,791 global / 742 owned diagnostics; remaining `BloomShaderManager` errors are in the old post-chain lifecycle. `git diff --check` passes.

- Bloom config/shader contract follow-up: `BloomShaderManager` now implements Configuration 4.1.3+26.2's `Validator<BloomType>`/`ValidationResult` API and uses 26.2 `ClickEvent.OpenFile(Path)` for its clickable log link. Removed only the stale `ShaderInstance` registrations and getters: project search found no consumers, and `GTRenderTypes` directly registers pipelines using the same bloom shader identifiers and target JSON shader definitions. `javap` on the exact cached Configuration jar and patched Minecraft `ClickEvent.java` confirm the contracts. Integrated offline compile `build/friend-client-port-bloom-config-verify.log`: 1,791 global / 742 owned diagnostics, down 19/19 from the preceding 1,810 / 761 checkpoint. `BloomShaderManager` now has only three remaining diagnostics, all from old `PostChain` allocation/resizing/target lookup. `git diff --check` passes.

- Migrated `MachineRenderState` to the 26.2 `StateHolder` constructor, which receives its property-key and value arrays, and supplied the machine state-definition function required by `StateHolder.codec`. Patched Minecraft source `StateHolder.java` and `StateDefinition.java` confirm the constructor, codec, and factory signatures. This preserves the existing machine state codec and state factory selection.

- Migrated `GTBoatRenderer` from the removed `BoatRenderer(Context, boolean)`/`ListModel` entity API to 26.2 `AbstractBoatRenderer`, `BoatRenderState`, and `SubmitNodeCollector`.
- Preserved the rubber/treated texture selection and uses each corresponding baked model layer in the extracted render state; retained regular/chest model variants, vanilla boat hurt/bubble transforms, water-patch geometry, light, and outline submission. Exact target `BoatModel` has `createBoatModel()` and `createChestBoatModel()` definitions; there is no `ChestBoatModel` class.
- Replaced the state-shard `GTRenderTypes` implementation with registered 26.2 `RenderPipeline` descriptions and immutable `RenderSetup`/`RenderType` values. Kept separate blend/cull/depth and geometry layouts for bloom block/entity passes, monitor and assembly quads, no-depth block highlights, triangle strip/fan GUI geometry, and textured GUI geometry. Bloom uses the custom bloom shaders, block/entity vertex snippets, sampler/lightmap/overlay setup, and a dynamic bloom output target. NeoForge source confirms custom pipelines register through `RegisterRenderPipelinesEvent` on the mod bus.
- Migrated `GTExplosiveRenderer` to extracted `ExplosiveRenderState`, `BlockModelResolver`, and deferred submit calls. It snapshots the explosive block model and fuse, preserves the TNT pulse/white-flash timing and block pose, and uses the same current `submitWhiteSolidBlock` path as vanilla TNT.
- Replaced `StaticFaceBakery`'s removed int-packed-vertex implementation with 26.2 `FaceBakery.bakeQuad`, `CuboidFace.UVs`, `BakedQuad.MaterialInfo`, `Material.Baked`, and `ExtraFaceData.DEFAULT`. Kept its AABB constants, cube-face UV mapping, model/UV transforms, texture key, tint, cull face, shade, and emissivity inputs. Target `FaceBakery` computes atlas UVs, winding, direction, normals, and baked colors.
- Rewrote `GTQuadTransformers` for record-style 26.2 `BakedQuad`: offset/copy/derotate/setSprite/setColor create immutable copies and preserve texture key, material metadata, colors, and normals. UV mapping uses target `UVPair.pack/unpackU/unpackV`; offset recalculates normals. `TextureMetadataHelper` reads target `materialInfo().sprite()` and compares its uniform `lightEmission` to ambient packed-light levels because the target quad has no per-vertex lightmap field.
- Removed `BakedQuadMixin`'s obsolete accessors-based equality/hash overrides; the 26.2 quad record supplies equality for current positions, UVs, direction, material, normals, and colors. Kept its texture-key extension. Migrated `GTTextureMetadata` from the removed serializer interface to target `MetadataSectionType` + `Codec` and `ResourceMetadata.getSection`.
- Migrated `TextureOverrideModel` from Forge's removed `BakedModelWrapper` to target `BlockStateModel` and `BlockStateModelPart`. It delegates state-aware and legacy part collection, wraps every part while preserving AO/material flags/particle material, retextures keyed quads, and provides a geometry key that includes the wrapped model, its contextual key, and immutable override map. Unkeyed quads are retained in the wrapper so non-overridden geometry is not lost.
- Target source evidence used: patched MC 26.2.0.88 `client/resources/model/cuboid/FaceBakery.java`, `CuboidFace.java`, `geometry/BakedQuad.java`, `model/geom/builders/UVPair.java`, `server/packs/metadata/MetadataSectionType.java`, and `server/packs/resources/ResourceMetadata.java`; NeoForge `ExtraFaceData` and baked-quad color/normal types.
- Quad mesh arrays are now converted through the explicit private CPU layout described above. Semantic follow-up: the old `QuadReInterpolator` still computes a temporary per-vertex lightmap for legacy callers, but target immutable `BakedQuad` has no per-vertex light field; verify that this compatibility scratch is never treated as target material lighting. Target material emission and submission-time world lighting are preserved independently.
- The render-type migration is complete in this worktree: `GTRenderTypes` now uses 26.2 registered pipelines and `RenderSetup`, so `RenderStateShard` is no longer needed. Remaining render consumers still use removed `MultiBufferSource`/immediate-draw APIs and must migrate to extracted state and collectors.
- Existing particle handoff confirms MufflerParticle/HazardParticle are already migrated elsewhere. This worktree starts from the clean integration base, so no particle changes from another uncommitted worktree are copied or overwritten.

## Verification

- Connected-texture model migration: `CTMBakedModel` now collects target `BlockStateModelPart`s, snapshots face connections into its geometry key, eagerly builds directional CTM quads, and preserves unculled quads and part material/AO metadata. `ModelEventHelper` and `AssetEventListener` now use 26.2 atlas-stitch, model-bake, standalone-model, and client-reload events; automatic CTM detection scans baked part sprites, and `MachineModel` is explicitly marked as handling its own CTM. Exact target contracts are from NeoForge `TextureAtlasStitchedEvent`, `AddClientReloadListenersEvent`, `ModelEvent.ModifyBakingResult`/`RegisterStandalone`, `BlockStateModelExtension`, and MC `ModelBakery.BakingResult`.
- Integrated offline compile: `build/friend-client-port-ctm-model-parts.log` recorded 1,903 global / 854 owned diagnostics with no errors in `CTMBakedModel`. The first model-event pass found only the unsupported event-bus annotation attribute; corrected in the follow-up verification `build/friend-client-port-model-events-verify.log`, which recorded 1,878 global / 828 owned diagnostics. No diagnostics remain in `ModelEventHelper`, `AssetEventListener`, `CTMBakedModel`, or `ManuallyConnectedTextureModel`. Six ModernFix integration diagnostics remain outside this assignment's ownership; request 6 records the required update. `git diff --check` passes.
- GUI/model consumer follow-up: `ToolChargeBarRenderer` now uses `GTRenderTypes.guiColor()`, `CTMMeshBuilder` reads the target quad sprite from `materialInfo()`, and `Mesh` uses the current block texture atlas accessor. `build/friend-client-port-quad-atlas-consumers.log` recorded 1,924 global / 875 owned diagnostics, with no errors in those files.
- Variant rotation follow-up: `VariantRotationHelpers` now uses target `Quadrant` and `BlockModelRotation` orientation composition for the custom machine-model path. `VariantDeserializerMixin` was removed because target `Variant.MAP_CODEC` handles native x/y/z and the legacy mixin target is absent; shared config cleanup is request 5. `build/friend-client-port-variant-rotations.log` recorded 1,915 global / 866 owned diagnostics, and the helper is diagnostic-free.
- Machine state follow-up: integrated offline compile `build/friend-client-port-machine-render-state.log` reports 1,874 global / 826 owned diagnostics, down two owned errors from the prior checkpoint. `MachineRenderState` has no remaining compiler diagnostics; overall compilation remains blocked by unrelated port errors. `git diff --check` passes.
- Cover atlas follow-up: `SimpleCoverRenderer` now checks the target `TextureAtlas.getTextures()` map for an optional emissive sprite before requesting it, preserving the existing fallback behavior. Integrated offline compile `build/friend-client-port-simple-cover-atlas.log` reports 1,873 global / 825 owned diagnostics, with no diagnostics in `SimpleCoverRenderer` or `MachineRenderState`. Full compilation still fails on unrelated port errors; `git diff --check` passes.
- Dynamic renderer codec follow-up: converted the eight owned dynamic-render subtype declarations to `MapCodec`/`RecordCodecBuilder.mapCodec`, matching target `Codec.dispatchStable`'s exact map-codec contract without changing the serialized fields. This clears the `DynamicRender.CODEC` dispatch error; those classes retain separate old drawing/model API diagnostics, so the broader renderer migration remains pending. Integrated offline compile `build/friend-client-port-dynamic-render-codecs.log` reports 1,867 global / 819 owned diagnostics. Also updated `IMachineRendererModel`'s render AABB conversion through `Vec3.atLowerCornerOf` to preserve its original block-aligned bounds; it is included in the next compile checkpoint.
- Atlas, bounds, and cover follow-up: `Mesh` now resolves the block atlas through 26.2 `Minecraft.getAtlasManager().getAtlasOrThrow(...)`, as confirmed by patched `Minecraft` and `AtlasManager` sources. `IOCoverRenderer` uses `BlockModelRotation.IDENTITY` for the prior zero rotation. `IMachineRendererModel` expresses its old block-aligned AABB through target `Vec3.atLowerCornerOf`, with its three separate removed-buffer API diagnostics remaining. `build/friend-client-port-atlas-and-cover-rotation.log` records 1,862 global / 814 owned diagnostics; `Mesh`, `IOCoverRenderer`, `SimpleCoverRenderer`, and `MachineRenderState` are diagnostic-free.
- Particle normal-pass follow-up: `GTParticleManager` now listens for the target `SubmitCustomGeometryEvent`, uses the frame's extracted camera/frustum and delta tracker, and submits through the target `SubmitNodeCollector`. Depth-write-disabled particles select a matching immutable render pipeline instead of mutating RenderSystem state. `GTOverheatParticle` snapshots its VoxelShape boxes and color/alpha before deferred vertex submission, preserving its per-face shading and color curve without retaining the mutable particle in the callback. `EffectRenderContext` reads target camera render-state fields while retaining a Camera overload for the still-pending bloom renderer. `build/friend-client-port-particle-submit-verify.log` records 1,837 global / 789 owned diagnostics; no errors remain in `GTParticle`, `GTParticleManager`, `EffectRenderContext`, or `GTRenderTypes`. At that checkpoint, six `GTOverheatParticle` errors remained in the legacy direct bloom setup; the follow-up at the top of this handoff resolves them. The full build remains blocked by unrelated port errors; `git diff --check` passes.
- Bloom event/cleanup follow-up: `BloomEventListeners` now uses NeoForge 26.2 `RenderFrameEvent.Pre` for the once-per-frame transparent bloom-target clear and `ClientTickEvent.Pre` for the former client-tick START check. The clear uses `RenderTarget.getColorTexture()` and `CommandEncoder.clearColorTexture`, so it does not bind the retired main framebuffer. Chunk cleanup reads the event's `LevelAccessor` and invalidates the inclusive `getMinSectionY()` through `getMaxSectionY()` range. Target NeoForge `RenderFrameEvent`, `ClientTickEvent`, and `ChunkEvent` sources plus patched MC `RenderTarget`/`LevelHeightAccessor` confirm these contracts. Compile `build/friend-client-port-bloom-events-verify.log` reports 1,817 global / 768 owned diagnostics; the new frame/tick/section code is diagnostic-free. Bloom rendering remains incomplete: 26.2 has no `RegisterNamedRenderTypesEvent`, and old bloom post-chain/shader/direct-draw APIs still account for most remaining bloom errors. `git diff --check` passes.
- Fluid/render utility follow-up: `RenderUtil.FluidTextureType` now resolves still, flowing, and optional overlay sprites from the target `FluidStateModelSet`/`FluidModel`, falling back from a missing overlay to the same fluid's still material. Its packed light now delegates to `LightCoordsUtil.getLightCoords`, retaining state self-emission and sky/block light. Updated owned `FluidBlockRenderer` and quantum-tank sprite calls. Pattern preview's similarly named `FluidTextureType` belongs to ModularUI and retains its existing `FluidState` calls. Patched MC `FluidStateModelSet`, `FluidModel`, and `LightCoordsUtil` plus NeoForge `IClientFluidTypeExtensions` confirm the contracts. `build/friend-client-port-fluid-models-verify2.log` reports 1,810 global / 761 owned diagnostics; helper callsites type-check, while these consumers retain separate removed render-buffer/model APIs. `git diff --check` passes.

- Fresh integrated offline compile after the boat migration: log `build/friend-client-port-boats.log`; 2,188 global diagnostics / 1,140 owned.
- Pipeline foundation compile: `build/friend-client-port-pipelines-verify.log`; 2,100 global / 1,052 owned. The first pass exposed 14 local typing/import issues, then exact fixes were applied and the verification pass cleared them.
- Latest integrated compile after pipeline and TNT renderer batches: `build/friend-client-port-tnt-renderers.log`; 2,090 global / 1,042 owned. No diagnostics remain in `GTRenderTypes`, `GTBoatRenderer`, or `GTExplosiveRenderer`.
- Latest integrated compile after bakery/quad/texture-metadata batches: `build/friend-client-port-quad-metadata-verify.log`; 1,974 global / 925 owned. No diagnostics remain in `StaticFaceBakery`, `GTQuadTransformers`, `BakedQuadMixin`, `TextureMetadataHelper`, or `GTTextureMetadata`.
- Latest integrated compile after the target model wrapper: `build/friend-client-port-texture-wrapper.log`; 1,966 global / 917 owned. No diagnostics remain in `TextureOverrideModel` either. Global compilation still fails on broad unrelated port errors.
- Fresh resume baseline: same integrated offline command; log `build/friend-client-port-resume-baseline.log`; 1,966 global / 917 owned, confirming the saved checkpoint before resuming.
- Migrated `GuiHeartTypeMixin` to the target `Hud$HeartType.forPlayer` hook and `Player.hasEffect(Holder<MobEffect>)`; patched HUD source confirms the poison call at ordinal 0 and the moved nested class. Verification log `build/friend-client-port-heart-mixin.log`; 1,964 global / 916 owned, zero diagnostics in that mixin.
- Audited obsolete client mixins against target sources. `FaceBakeryMixin` now targets the exact 26.2 `FaceBakery.bakeQuad(ModelBaker, Vector3fc, Vector3fc, CuboidFace, Material.Baked, ...)` overload and records `CuboidFace.texture()`. Target `UnbakedCuboidGeometry.bake` confirms this is the vanilla cuboid path. Removed the obsolete `BlockModelMixin` and unused `RenderStateShardAccessor`; the shared `gtceu.mixins.json` entries are integration requests because that file is outside this assignment's ownership.
- Migrated `FakeBlockTintGetter` to target `client.renderer.block.BlockAndTintGetter`, `cardinalLighting()`, and `LevelHeightAccessor` names. It still substitutes block/state/entity only at its configured position and delegates other data to its parent. Latest verification log `build/friend-client-port-fake-tint-world-verify.log`; 1,926 global / 877 owned. No diagnostics remain in `FakeBlockTintGetter`, `FaceBakeryMixin`, or `GuiHeartTypeMixin`.
- All counts use `compileJava -PportDiagnostics --max-workers=1 --console=plain --offline`; global compilation still fails on broad unrelated port errors. `git diff --check` passed.
- No tests or in-game rendering verification run.

## Next

1. Continue multipart and machine model migration after the target custom `BlockStateModel`/machine-BER association and thread-safe model-data contract in request 12 are supplied; do not use live machine block entities from chunk meshing.
2. Continue pipe models via request 10, then facade/item wrappers and cover rendering while preserving model parts, tint, culling, and render-state snapshots.
3. Continue bloom's remaining safe-mode/chunk-buffer and ticket-submission paths. Requests 13 captures the absent named-render-type contract; keep PNG metadata bloom and safe-mode output alive.
4. Apply the existing shared listener, item-renderer, and mixin-config requests when their owning integration changes are available.
5. Re-run the integrated offline diagnostic compile after each substantial behavior-preserving batch; runtime checks remain pending until the project builds.

## Risks and review

- Full project compilation, game startup/resource reload, render visuals, and mixin application are not verified. Runtime checklist: machine/pipe/facade/preview geometry, boat variants, bloom on/off/resize, particle motion/fade/lighting, reload cleanup.
- Shared registration/event code remains unowned; integration requests are in `FRIEND_CLIENT_INTEGRATION_REQUESTS.md`.
- AI-assisted work by OpenAI Codex. Project `AI_POLICY.md` requires human understanding/review before upstream contribution; no work is represented as human-reviewed.
