# Client Port Plan

## Active ownership (2026-09-24 takeover)

Current checkout is `1.20.1` at `a9176b5c9f3b7eb422bdb65767ee0991b99936e8`; the historical baseline below refers to an older integrated commit. The fresh offline Java 25 compile reached javac at `build/client-takeover-baseline-jdk25-escalated-20260924.log`: 718 global / 370 exact-owned diagnostics. The latest integrated GUI/bloom/mixin compile at `build/client-gui-bloom-integrated-verify-20260924.log` also reached javac and remains 718 / 370; the edited GUI/bloom/mixin paths add no diagnostics. Both full builds fail on the remaining port errors. `git diff --check` passes; no tests or runtime visuals have run.

| Owner | Exclusive writable scope | Task |
| --- | --- | --- |
| Sol coordinator | Client architecture, bloom review, and the friend plan/handoff/integration documents | Establish current baseline, review model contracts, and integrate verified work. |
| Luna facade (completed; no edits) | No current writable scope | The legacy facade item and cover routes remain referenced by live shared registration and resources; requests 17 and 19 must switch those producers first. |
| Luna pipe (completed; no edits) | No current writable scope | The legacy pipe loader remains referenced by the shared builder and generated resources; requests 10, 21, and 22 gate its retirement. |
| Luna mixins (completed; no edits) | No writable scope | Found removed GUI, player-skin, model-bakery, and bloom mixin targets plus stale/missing JSON entries; requests 24–25 and the handoff record the evidence. |
| Luna GUI (completed; reviewed) | No current writable scope | Migrated research item, tooltip, and bar extraction to 26.2 APIs. The obsolete `GuiGraphicsAccessor` was removed after review; its shared JSON registration must be removed before startup. |

Both Luna scopes exclude shared registration, generators, API/common code, resources, and Git operations. Only the coordinator runs Gradle. This table records assignments; completion awaits diff and caller review.

## Baseline

- Active IDE checkout: `1.20.1` / `C:\Users\georg\IdeaProjects\PatrickStarTech`; source worktree: `codex/friend-client-port` / `C:\Users\georg\.codex\worktrees\codex-friend-client-port\PatrickStarTech`.
- Current integrated commit: `ae73bb7c7d85d2b609c6f4e0a1f5195bc10da61c`. Both worktrees start at this commit. The dedicated client worktree remains preserved; its 55 existing changed/new files were copied into the clean local IDE checkout so the user can access the port work there. No commit was created.
- Target config: Minecraft 26.2, NeoForge 26.2.0.88, Java toolchain 25; patched MC source archive at `build/moddev/artifacts/minecraft-patched-26.2.0.88-sources.jar` matches.
- Newest verification: `build/friend-client-port-ctm-wrapped-machine-ber-verify.log`; javac reports **718 global errors / 370 exact-owned diagnostics** (367 assigned client paths + 3 client mixins), unchanged from the prior local pass. `MachineBlockEntityRenderer` now resolves target machine models through the CTM-unwrapping helper; the changed BER class adds no diagnostics. The full project compile still fails on remaining port APIs. No tests or runtime visuals ran.
- Fresh resume baseline: `compileJava -PportDiagnostics --max-workers=1 --console=plain --offline`; log `build/friend-client-port-resume-20260924-baseline.log`; 891 compiler error lines / 891 global errors, 544 exact-owned-path diagnostics. The command reached javac and failed on the remaining port errors; no build flags or annotation processing were changed.
- Other contributor's pre-transfer compile checkpoint: `build/friend-client-port-pipe-datagen-builder-verify2.log`; **830 global errors / 483 owned diagnostics** on that source revision. Its counts are historical and are not comparable with this local checkout's current log. Request 21 retains the shared active-pipe emissivity contract.

## Ownership

Java under `client/renderer`, `client/model`, `client/util`, `client/bloom`, `client/particle`, and `core/mixins/client`; focused tests under `src/test/java/com/gregtechceu/gtceu/client`; this plan, handoff, integration requests; only directly required text shaders. Shared registration/common/config/build/other workers' files are read-only.

## Queue

1. **Done — render type definitions.** `GTRenderTypes` now describes and registers distinct 26.2 pipelines with `RenderSetup`; diagnostic-free in the latest integrated pass. Its consumers still need conversion from removed `MultiBufferSource`/manual draw APIs.
2. **Active — remaining model geometry.** The standalone face bakery, immutable quad transforms, baked-quad extension, texture metadata codec, `TextureOverrideModel`, fake tint world, vanilla face-texture key hook, private quad mesh bridge, growing-plant property/stem-state setup, and pipe datagen builder types now use target/project contracts. Target machine, pipe, facade, and ordinary-cover models are implemented; producer/schema/atlas wiring remains. The pipe generator still emits the old custom-loader model JSON until request 10 switches it to the target `gtceu:pipe` block-state model; request 21 covers the shared per-face emissivity metadata gap. Continue with remaining independent model wrappers and reload behavior; request 12 covers the machine model-data/BER integration. Request 16 records the shared mixin registration needed for custom stem fruit.
3. **Done — previews/highlights.** Pattern, block-outline, and timed AABB previews now submit from extracted render state. Shared listener cleanup remains queued (requests 7–9).
4. **Active — bloom completion.** Frame/tick hooks, post-chain config, settings UBO refresh, framegraph execution, main-target compositing, persistent tickets, and vanilla static section geometry use target APIs. Remaining work is external `gtceu:bloom` named-render compatibility (request 13), shared mixin-config cleanup (request 23), the pinned optional Embeddium path (request 15), and shared particle-debug wiring.
5. **Active — dynamic item route.** Quantum chest/tank special-renderer snapshot providers and the shared `gtceu:machine_dynamic` special renderer are implemented and compiler-clean. Machine item definition generation and removal of the legacy `MetaMachineItem` custom renderer remain integration request 20.
6. **Audit.** Owned client mixin descriptors, registrations requested from shared owners, focused geometry checks; then compile/runtime checklist.

Cross-cutting dependency: 26.2 uses registered `RenderPipeline` + immutable `RenderSetup`/`RenderType` descriptions and extracted node submission; old state shards, immediate buffer builders, and render-stage callbacks do not carry over. Implement a small pipeline/geometry path with vanilla/NeoForge source evidence before migrating broad consumers.

## Status

- **Machine BER wrapped-model lookup:** `MachineBlockEntityRenderer` now uses `MachineBlockStateModel.find()` at extraction, submission, visibility, and bounds lookup. This recognizes CTM-wrapped machine models and keeps descriptor snapshots associated with the matching machine definition. Compile `build/friend-client-port-ctm-wrapped-machine-ber-verify.log`: **718 global / 370 exact-owned diagnostics**, unchanged; the BER file has no diagnostics. The compile reached javac and failed on the project’s remaining APIs. No runtime model/BER checks ran.

- Newest verified batch: `GrowingPlantRender` now uses target property value bounds and a `StemBlock` fruit-key accessor; configure-only lambdas match the simplified state/progress signature. Compile `build/friend-client-port-growing-plant-lambda-fix-verify.log` reports **1,584 global / 535 owned diagnostics**, down 6 / 6; its six remaining errors are the old `MultiBufferSource` boundary. Request 16 records shared mixin registration. No tests or runtime visuals ran.
- Current machine-model slice: `MachineBlockStateModel` implements the NeoForge 26.2 `DynamicBlockStateModel` worker contract and selects variant/multipart `BlockStateModel` parts from the `GTModelProperties.MACHINE_RENDER_STATE` snapshot. `UnbakedMachineBlockStateModel` has a target custom codec, resolves child dependencies per resource bake, preserves weighted child models and recursive AND/OR multipart predicates, and uses the default render state when model data is absent or stale. `MachineBlockStateModelType#register(RegisterBlockStateModels)` owns target codec registration. The legacy model/loader is not yet replaced, and the model-data producer, target BER, item route, and generated model schema still need wiring. Verification log `build/friend-client-port-machine-state-model-verify3.log`: **891 global / 544 exact-owned diagnostics**, unchanged from the fresh baseline; both new model classes have no diagnostics. `git diff --check` passes. No runtime checks ran.
- Current pipe-model slice: `PipeBlockStateModel` and `UnbakedPipeBlockStateModel` register the target custom model codec and collect center, connection, restrictor, copied frame, facade, and ordinary cover parts. Frame parts use the target frame `BlockState` for tint sources and skip covered cull faces. A copied painted color is applied to pipe-child tint layers 0/1 and those tint indices are cleared; unpainted child and frame colors stay on block tint sources. The geometry key includes masks, frame, paint, and cover-side snapshots; absent connection data still falls back to center geometry only and its key tracks paint. Active connection appearance remains blockstate-selected. The shared producer, block-atlas directory source, old loader/baked model, and generated JSON wiring remain pending. No runtime visuals ran.
- Completed: checkout/rules/version/source verification and fresh diagnostic baseline; `GTRenderTypes`, `GTBoatRenderer`, `GTExplosiveRenderer`, `StaticFaceBakery`, `GTQuadTransformers`, `BakedQuadMixin`, `GTTextureMetadata`, `TextureOverrideModel`, `FakeBlockTintGetter`, `FaceBakeryMixin`, `GuiHeartTypeMixin`, `HumanoidArmorLayerMixin`, `ArmorItemModelGenerator`, `GTTankItemFluidPreview`, `ToolChargeBarRenderer`, `CTMMeshBuilder`, `Mesh`, `VariantRotationHelpers`, `CTMBakedModel`, `AssetEventListener`, `ModelEventHelper`, `MachineRenderState`, `SimpleCoverRenderer`, `IOCoverRenderer`, `GTParticle`, `GTParticleManager`, `EffectRenderContext`, `BlockHighlightRenderer`, `AABBHighlightRenderer`, `PatternPreviewRenderer`, `FluidBlockRenderer`, `GTBloomParticle`, `GTOverheatParticle`, `RenderBufferHelper`, and `ModelManagerMixin` are diagnostic-free in the latest integrated compile. Bloom frame/tick hooks, safe-mode section cleanup, post-chain config, settings UBO refresh, framegraph execution, resize lifecycle, and main-target compositing use target APIs. `RenderUtil` fluid texture/light lookup uses target fluid models and `LightCoordsUtil`; owned sprite callsites are updated. `IMachineRendererModel`'s AABB bounds use target `Vec3` conversion; its three removed-buffer API diagnostics remain. Dynamic render subtype codecs use target `MapCodec` variants while their rendering API errors remain. The overheat particle submits normal and emissive bloom geometry through the target collector with immutable shape/color snapshots; both paths are diagnostic-free. GUI horizontal gradients and AoE break/outline geometry now use extracted-state submission. Armor tint uses the target per-layer ARGB extension hook; the tank preview uses the transfer handler and extracted GUI stratum. The multipart selector cache retains identity-key semantics via `IdentityHashMap`. Previous integrated compile checkpoint `build/friend-client-port-colored-aoe-outline-verify2.log`: 1,600 global / 551 exact-owned diagnostics, down 596 / 597 from baseline; `git diff --check` passes.
- Active: large remaining model families are `FacadeCoverRenderer` (46), `MultiPartBakedModel` (39), `CustomItemRendererWrapperModel` (33), machine model/loader (53 plus multipart), and pipe models. These use obsolete model-loader/block-renderer associations; requests 10 and 12 are shared blockers. The 10 `RenderUtil` diagnostics are old block-model/direct-draw helpers coupled to that same machine/dynamic-render migration. Lamp special-item hookup and client mixin-config cleanup are requests 11 and 14.
- Bloom remaining: the post-chain/framegraph migration is implemented. Safe-mode per-section chunk buffers and bloom chunk mixins still use removed direct APIs; patched 26.2 chunk compilation exposes only fixed solid/cutout/translucent layers and has no `LevelRenderer.renderChunkLayer(RenderType, ...)` entry point for the old output bridge. The named `gtceu:bloom` contract is absent; the Embeddium bloom mixins compile against a pinned 1.20.1 dependency and lack a verified target quad-emission contract (request 15). Requests 13 and 15 record the missing integration surfaces. Do not route bloom geometry to normal-world layers or remove bloom to clear these diagnostics.
- Blocked: runtime validation/full build until global compiler errors and shared integration blockers are resolved.

- Facade item-model slice: added `FacadeItemModel` as a registered 26.2 `ItemModel` with a resolved base-model dependency, display-transform/particle/light properties, item tint-source layers, foil, and a reload-scoped facade-quad cache. It reads the stack’s facade block state, collects its target block-state parts, preserves north-face filtering, bakes item-context block tint, clips to the inset north plane, and re-interpolates UVs. Verification `build/friend-client-port-facade-item-model-verify2.log`: **878 global / 530 exact-owned diagnostics**, unchanged from the lamp checkpoint; both new facade item classes have zero diagnostics. The new model-data item definition/base asset and legacy loader cleanup remain outside this assignment; request 19 specifies them. Dynamic block-entity/model-data-dependent facade item appearances are not verified. No tests or visuals ran.
- Lamp item special-renderer slice: migrated `LampItemRenderer` to target `SpecialModelRenderer<BlockState>`, added a registered `gtceu:lamp` special-renderer codec, and resolves the lamp’s stack-selected block state into the active 26.2 `BlockStateModelSet`. It submits contextual model parts, block tint-source colors, item foil, and unit-block extents through target render submission. Compile `build/friend-client-port-lamp-special-item-verify.log`: **878 global / 530 exact-owned diagnostics**, down 13 / 14 from the fresh baseline; the new lamp renderer classes have no diagnostics. The old `LampBlockItem#initializeClient` and `GTBlocks` custom-loader asset generation are outside this ownership and remain to be wired; request 18 specifies the exact target model change. No tests or in-game item visuals ran.
- Latest facade cover slice: added the registered `gtceu:facade` target block-state model and immutable `FACADE_RENDER_STATE` entries. It consumes per-face child snapshots from `COVER_MODEL_DATA`, looks up the current reload generation's facade block-state model while collecting parts, preserves attached-side clipping/UV interpolation and tint-source context, and stores no live block entity or baked model reference. Verification `build/friend-client-port-facade-model-verify2.log`: **891 global / 544 exact-owned diagnostics**, unchanged from the fresh baseline; the new facade model classes have zero diagnostics. The item `FacadeUnbakedModel`/item display route is still legacy and the unowned producer/generated-schema wiring is documented in request 17. No tests or visuals ran.

## Current resume checkpoint

- **Static block bloom geometry:** `BloomChunkGeometry` captures metadata-selected quads from target `BlockStateModelSet` models during NeoForge `AddSectionGeometryEvent`, preserving 26.2 baked colors, contextual tint/shading, per-vertex light plus material emission, UVs, overlays, and face normals in immutable primitive arrays. It submits only snapshots for visible renderable sections via `SubmitCustomGeometryEvent` and `GTRenderTypes.bloom()`. Revision/level-generation tokens prevent stale rebuild callbacks from repopulating invalidated sections; chunk unload, level unload, and client resource reload clear snapshots. `BloomRenderer` now only runs the verified framegraph post effect and keeps a geometry-backed compatibility capture surface for the optional integration. Patched MC `SectionCompiler` calls NeoForge additional renderers while its `ModelBlockRenderer` and lighting cache are live; NeoForge event sources verify the worker and per-frame collector contracts. Removed the obsolete vanilla bloom mixins and `LevelRendererAccessor`; shared `gtceu.mixins.json` cleanup is request 23. The legacy named render-type registration is removed because the target API is absent and repository search found no in-tree block/item model definition using that alias; compatibility for external assets remains request 13. Verification `build/friend-client-port-bloom-section-geometry-verify2.log`: **718 global / 370 exact-owned diagnostics**, down 62 / 62 from 780 / 432; `BloomChunkGeometry`, `BloomRenderer`, and `BloomEventListeners` have zero diagnostics. The three remaining client-mixin diagnostics are in the pinned 1.20.1 Embeddium integration (request 15). No tests or runtime visuals ran. The new path retessellates model geometry once per section rebuild; runtime performance and render appearance are unverified.
- **Weighted CTM descendants:** Added `CTMWeightedVariants`, which delegates weighted choice, contextual model keys, material flags, and part collection to 26.2 `WeightedVariants`, while exposing all nested baked candidates. Machine and pipe codecs use it recursively; both custom models now implement `CTMModelPartSource` across their inline variant/multipart children. Patched MC 26.2 `WeightedVariants` confirms normal collection chooses a single weighted child. Verification `build/friend-client-port-pipe-weighted-ctm-verify.log`: **780 global / 432 exact-owned diagnostics**; changed machine/pipe/CTM classes add no diagnostics. `git diff --check` passes. No tests or runtime CTM visuals ran.
- **Ordinary covers:** Added immutable `CoverRenderState` capture for non-facade Simple/I/O covers and `CoverBlockStateModel` composition into machine and pipe parts, geometry keys, and material flags. Simple covers retain the optional `_emissive` companion rule; I/O covers retain inversion fallback and forced full-bright overlays; occupied faces and backplates use copied thickness/flags. The shared producer and atlas source are tracked in request 22. Compile `build/friend-client-port-static-cover-parts-verify.log`: **816 global / 468 exact-owned diagnostics**, unchanged from the saved bloom log; the new source files and composition points add no diagnostics. `git diff --check` passes. No tests or visuals ran.
- **Dynamic snapshot contract:** `DynamicRender` now supplies target visibility/bounds and immutable extraction/submission independently of the obsolete immediate-buffer renderer interface. Removed duplicate immediate callbacks for assembly lines, fluid areas, fusion rings, growing plants, quantum chest/tank visuals, and boiler part models where target snapshots or `ControllerPartModel` already cover the output. `GrowingPlantRender.RenderFunction` now only selects configured block states/offsets; the target snapshot applies scale/orientation and target model tint/light. Compile `build/friend-client-port-dynamic-snapshot-contract-verify.log`: **780 global / 432 exact-owned diagnostics**, down 29 global / 29 exact-owned from the preceding compile; none of these new target paths report diagnostics. `MachineModel`/`UnbakedMachineModel` still belong to the old loader path pending request 12. `git diff --check` passes. No tests or visuals ran.

- Latest integrated offline compile: `build/friend-client-port-dynamic-cover-text-verify.log`, **877 global errors** (538 client-path / 17 client-mixin-path diagnostics). The target cover extraction, machine-state capture, and deferred text submission add no diagnostics; legacy cover callbacks remain. No tests or runtime visuals ran.
- Latest integrated offline compile: `build/friend-client-port-quantum-chest-item-state-verify.log`, **877 global errors** (538 client-path / 17 client-mixin-path diagnostics). The target quantum-chest snapshot resolves and submits a `ItemStackRenderState` plus amount text without diagnostics; remaining errors in that renderer are legacy immediate/item APIs. No tests or runtime visuals ran.
- Latest integrated offline compile: `build/friend-client-port-fusion-ring-submission-verify.log`, **877 global errors** (538 in `client/`, 17 in `core/mixins/client/`). Fusion ring extraction/submission, the dedicated bloom output pipeline, and the pose-based ring helper are diagnostic-free. Old ticket-based bloom and legacy machine callbacks still fail; target registration/runtime visuals remain unverified. No tests ran.
- Latest integrated offline compile: `build/friend-client-port-quantum-tank-snapshot-verify2.log`, **877 global errors** (538 diagnostics in `client/`, 17 in `core/mixins/client/`). Target quantum-tank fluid extraction, geometry, and text submission are diagnostic-free; the legacy callback and item path still fail on removed APIs. This checkpoint is one global error below the earlier 878 state; no tests/runtime visuals ran.
- Fluid-area migration: `FluidAreaRender` captures fluid identity, 26.2 fluid-model transparency, selected planes, copied offsets, and per-offset light before deferred submission. The callback uses the current block atlas and does not retain a world reference. Verification `build/friend-client-port-fluid-area-snapshot-verify3.log`: **877 global errors; 538 client-path and 17 client-mixin-path diagnostics**. New snapshot and light-helper methods have no diagnostics; legacy callbacks and broad unported APIs still fail the full compile. No tests or runtime visuals ran.
- Previous integrated offline compile: `build/friend-client-port-formed-boiler-parts-verify2.log`, **878 global errors** (539 client-path and 17 client-mixin-path diagnostics by the same path filter). It reached javac and failed. The target machine bridge, assembly-line snapshot, growing-plant submission, and formed-boiler part-model hook added no diagnostics. No tests or runtime visuals ran.
- Latest integrated offline compile `build/friend-client-port-machine-ber-assembly-line-verify2.log`: **878 global / 530 exact-owned diagnostics**. The target `MachineBlockEntityRenderer` extracts descriptor-indexed machine snapshots and submits through `SubmitNodeCollector`; assembly-line laser data is the first descriptor using that boundary. New bridge/snapshot classes are diagnostic-free. Other dynamic descriptors and covers still lack target snapshots, so shared registration remains gated; the old `MultiBufferSource` callback remains in the compiler error set.
- Earlier machine CTM discovery verification `build/friend-client-port-machine-ctm-candidates-verify.log`: **878 global / 530 exact-owned diagnostics**. `MachineBlockStateModel` exposed baked variant/multipart candidates before a default machine state is chosen. At that checkpoint nested weighted models were sampled; the current resume checkpoint above replaces that behavior with recursive weighted-choice enumeration.
- Latest integrated offline compile `build/friend-client-port-machine-output-overlays-verify.log`: **878 global / 530 exact-owned diagnostics**. `MachineBlockStateModel` now recreates the legacy item/fluid output-face pipe overlay and optional auto-output overlays from `MACHINE_OUTPUT_RENDER_STATE`; the immutable snapshot is part of the geometry key, the three sprites are model dependencies, and their material flags are included. The shared producer must still publish supported directions and auto flags.
- Latest integrated offline compile `build/friend-client-port-facade-backplate-verify3.log`: **878 global / 530 exact-owned diagnostics**. The facade model now also includes generated backplate material flags in its model-level flags, preserving the target chunk layer for translucent or animated sprite content. Target facade classes remain diagnostic-free; the full build still fails on broad legacy APIs.
- Latest integrated offline compile `build/friend-client-port-facade-backplate-verify2.log`: **878 global / 530 exact-owned diagnostics**. The target facade model now bakes its backplate sprite as a reload dependency and emits back/exposed-side plate quads from immutable thickness and occupied-face snapshots, retaining the legacy inset and face-culling rules. The shared model-data producer still needs to publish those two values. The full compile remains failed on broad legacy APIs.
- Latest integrated offline compile `build/friend-client-port-machine-texture-dependencies-verify.log`: **878 global / 530 exact-owned diagnostics**. `UnbakedMachineBlockStateModel.resolveDependencies` now marks each `texture_overrides` identifier so the 26.2 atlas bake includes sprites later resolved by `ModelBaker.materials()`. The full compile still fails in broad legacy APIs; the target machine, pipe, and facade model classes have no diagnostics.
- Pipe model diagnostic pass `build/friend-client-port-pipe-paint-key-verify.log`: **877 global / 530 exact-owned diagnostics**. The target pipe model handles copied frame state and painted child tint while preserving center-only missing-mask geometry and paint invalidation; producer wiring and actual cover geometry remain open.

- Latest integrated offline compile `build/friend-client-port-machine-formed-part-textures-verify.log`: **877 global / 530 exact-owned diagnostics**. The target machine model retains dynamic descriptors and resolved override sprites, maps the legacy formed-part texture slots, and applies them through `retextureFormedPartQuads`; target BER consumption and shared model-data/schema wiring remain open. The compile still fails in the legacy machine implementation and broad unrelated 26.2 APIs.
- Resume compile at integrated commit `ae73bb7c7`: `build/friend-client-port-resume-20260924-baseline.log`, 891 global / 544 exact-owned diagnostics. Machine state-model slice verification: `build/friend-client-port-machine-state-model-verify3.log`, also 891 / 544; new target classes are diagnostic-free, while the old machine loader/model classes remain in the error set.
- Pipe model snapshot verification: `build/friend-client-port-pipe-model-state-verify.log`, also 891 / 544; the new connection/restrictor model classes are diagnostic-free. No count reduction is claimed because existing model classes and shared integrations remain.

- Latest integrated offline compile `build/friend-client-port-growing-plant-lambda-fix-verify.log`: **1,584 global / 535 owned diagnostics**, down 6 / 6 from the monitor NBT checkpoint. The full project build still fails on broad unported APIs.
- Latest integrated offline compile `build/friend-client-port-monitor-quad-nbt-defaults-verify.log`: **1,590 global / 541 owned diagnostics**, down 4 / 4 from the light-ring checkpoint. The full project build still fails on broad unported APIs.
- Latest integrated offline compile `build/friend-client-port-fusion-ring-pipeline-verify.log`: **1,594 global / 545 owned diagnostics**, down 6 / 6 from the previous 1,600 / 551 checkpoint. The full project build still fails on broad unported APIs.
- Refreshed integrated offline compile `build/friend-client-port-resume-20260924-gradient-baseline.log`: **1,605 global / 556 owned diagnostics**. After GUI-gradient submission, `build/friend-client-port-gui-gradient-verify.log`: **1,603 / 554**; after AoE break/outline extraction, `build/friend-client-port-colored-aoe-outline-verify2.log`: **1,600 / 551**. Relative to the original fresh baseline of 2,196 / 1,148, this is 596 / 597 lower; the full project compile still fails globally.
- The bloom `PostChainConfig`/GLSL, target framegraph execution, render-target lifecycle, quad lightmap audit, GUI-gradient submission, and AoE break/outline submissions are implemented. The quad audit files, `ToolChargeBarRenderer`, `AoEBlockBreakingRenderer`, and `ColoredAoEBlockOutlineRenderer` have no diagnostics. `git diff --check` passes. No tests or runtime visuals were run.
- Current blockers: the machine/multipart model path needs the target `BlockStateModel` codec and machine `BlockEntityRenderer` association described in request 12. The old named bloom type needs a target resource/model route described in request 13. Shared mixin-config cleanup for obsolete `LevelRendererMixin` is request 14. Safe-mode per-section bloom buffers and ticket callbacks still use old direct-render APIs, and Embeddium requires a 26.2-compatible artifact/API before its quad metadata migration is safe (request 15).
