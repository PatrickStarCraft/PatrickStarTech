# Client Port Integration Requests

## Open requests

### 1. Register particle-count debug entry

- **File:** `src/main/java/com/gregtechceu/gtceu/client/ClientProxy.java` (or the existing client mod-bus listener it owns).
- **Current contract:** `GTParticleManager` exposes `registerDebugEntries(RegisterDebugEntriesEvent)` for 26.2; the former `CustomizeGuiOverlayEvent.DebugText` callback no longer exists.
- **Required change:** Subscribe to NeoForge `RegisterDebugEntriesEvent` on the client mod event bus and call `GTParticleManager.INSTANCE.registerDebugEntries(event)`. Preserve the back/front particle-count lines as implemented by the owned manager.
- **Evidence:** `FRIEND_PARTICLES_HANDOFF.md` records migration of the manager to `RegisterDebugEntriesEvent`; 26.2 NeoForge replaces the old debug overlay event. Read and verify the current `GTParticleManager` method before wiring.
- **Blocks:** debug behavior only; not client compilation/startup.
- **Owned dependency:** `client/particle/GTParticleManager.java`.

### 2. Register current boat model layer definitions

- **File:** `src/main/java/com/gregtechceu/gtceu/client/ClientProxy.java`, client model-layer registration method.
- **Current contract:** Registers `BoatModel::createBodyModel` for boat variants and uses the removed `net.minecraft.client.model.ChestBoatModel::createBodyModel` for chest variants.
- **Required change:** Keep registering `GTBoatRenderer.getBoatModelName(type)` with `BoatModel::createBoatModel`; register `GTBoatRenderer.getChestBoatModelName(type)` with `BoatModel::createChestBoatModel`. Continue registering the four rubber/treated × regular/chest layer IDs.
- **Evidence:** Patched Minecraft 26.2.0.88 source `net/minecraft/client/model/object/boat/BoatModel.java` has `createBoatModel()` and `createChestBoatModel()`; no `ChestBoatModel` class exists. The renderer now bakes these named layers and stores each corresponding model/texture in its extracted render state.
- **Blocks:** full client compilation and startup for the boat renderer; the renderer is otherwise migrated.
- **Owned dependency:** `client/renderer/entity/GTBoatRenderer.java`.

### 3. Remove obsolete block-model mixin registration

- **File:** `src/main/resources/gtceu.mixins.json`, entry `client.BlockModelMixin`.
- **Current contract:** The entry refers to the owned `BlockModelMixin`, which injected at `BlockModel.bakeFace` and read the old `BlockElementFace.texture` field.
- **Required change:** Remove the `client.BlockModelMixin` entry. The owned mixin class has been removed because target 26.2 `BlockModel` is an update interface and no longer contains `bakeFace`; vanilla cuboid model faces now pass through `UnbakedCuboidGeometry.bake` to `FaceBakery.bakeQuad`, where the surviving `FaceBakeryMixin` records `CuboidFace.texture()` on the baked quad.
- **Evidence:** Patched Minecraft 26.2.0.88 source `client/renderer/block/model/BlockModel.java` declares only `update(...)` and baking context; `client/resources/model/cuboid/UnbakedCuboidGeometry.java` resolves each `CuboidFace` material and calls `FaceBakery.bakeQuad(modelBaker, from, to, face, ...)`. `FaceBakeryMixin` targets that exact overload.
- **Blocks:** client startup/mixin application while the stale entry remains; Java compilation is unaffected.
- **Owned dependency:** `core/mixins/client/FaceBakeryMixin.java` preserves texture-slot metadata for target cuboid baking.

### 4. Remove obsolete render-state-shard accessor registration

- **File:** `src/main/resources/gtceu.mixins.json`, entry `client.RenderStateShardAccessor`.
- **Current contract:** The entry refers to an accessor for `RenderStateShard.name`; the owned accessor had no in-repository callers.
- **Required change:** Remove the `client.RenderStateShardAccessor` entry. Its owned Java class has been removed; current render descriptions use `RenderPipeline` and `RenderSetup` and do not inspect legacy render-state names.
- **Evidence:** Patched Minecraft 26.2.0.88 source has no `net.minecraft.client.renderer.RenderStateShard`; repository-wide search found no callers beyond the accessor declaration. The migrated `GTRenderTypes` registers named target pipelines directly.
- **Blocks:** client startup/mixin application while the stale entry remains; Java compilation is unaffected.
- **Owned dependency:** the completed `client/renderer/GTRenderTypes.java` migration no longer needs this accessor.

### 5. Remove obsolete vanilla-variant deserializer mixin registration

- **File:** `src/main/resources/gtceu.mixins.json`, entry `client.VariantDeserializerMixin`.
- **Current contract:** The entry targets the removed `net.minecraft.client.renderer.block.model.Variant.Deserializer` to add a `gtceu:z` field to ordinary blockstate variants.
- **Required change:** Remove the `client.VariantDeserializerMixin` entry. The owned mixin source has been removed. Ordinary generated blockstate variants now encode `x`, `y`, and `z` through Minecraft 26.2's native `Variant.SimpleModelState` codec; the separate `gtceu:z` field remains handled by `VariantState.Deserializer` for custom machine-model payloads.
- **Evidence:** Patched Minecraft 26.2.0.88 source `client/renderer/block/dispatch/Variant.java` defines the native `x`, `y`, and `z` quadrant fields and `MAP_CODEC`; `BlockStateModel.Unbaked.ELEMENT_CODEC` uses `Variant.MAP_CODEC`. Current `GTBlockstateProvider` documents this split, `ConfiguredModel` emits native `z`, and `MachineModelBuilder` emits `gtceu:z` only for its custom payload. Repository search found no resource JSON using the legacy mixin property.
- **Blocks:** client startup/mixin application while the stale target entry remains; Java compilation is unaffected.
- **Owned dependency:** `client/util/VariantRotationHelpers.java` now uses target `Quadrant`/`BlockModelRotation` orientation composition for custom machine-model rotations.

### 6. Migrate the ModernFix dynamic-model callback

- **File:** `src/main/java/com/gregtechceu/gtceu/integration/modernfix/GTModernFixIntegration.java`, `onBakedModelLoad`.
- **Current contract:** The integration implements the old per-model callback with `BakedModel`, `UnbakedModel`, `ModelState`, and `Material`, then dispatches `AssetEventListener.BakedModelReplacement`. Those 1.20 model types and the listener interface no longer exist in the target path.
- **Required change:** Use the exact ModernFix version's supported dynamic-resource hook for 26.2 `BlockStateModel` results and invoke the corresponding client replacement listeners, or remove the obsolete integration callback and let the supported NeoForge `ModelEvent.ModifyBakingResult` path run. Do not disable ModernFix dynamic resources or connected textures as a workaround. Keep the opt-out in `ModelEventHelper` only while an equivalent dynamic-resource path is provided.
- **Evidence:** `build/friend-client-port-model-events-verify.log` shows six errors in this unowned file. NeoForge 26.2.0.88 `ModelEvent.ModifyBakingResult` exposes a `ModelBakery.BakingResult` whose block model registry is `Map<BlockState, BlockStateModel>`; `GTModernFixIntegration` still uses the removed 1.20 callback signature and removed `AssetEventListener.BakedModelReplacement` type.
- **Blocks:** full compilation; connected-texture replacement when ModernFix dynamic resources are enabled.
- **Owned dependency:** `client/util/ModelEventHelper.java` now wraps target `BlockStateModel`s in the normal NeoForge model-bake event and exposes `AssetEventListener.BlockStateModelReplacement`.

### 7. Replace the legacy block-highlight event call

- **File:** `src/main/java/com/gregtechceu/gtceu/client/ClientEventListener.java`, `onBlockHighlightEvent`.
- **Current contract:** The listener subscribes to the removed `RenderHighlightEvent.Block`, passes its `MultiBufferSource` to `BlockHighlightRenderer.renderBlockHighlight`, and depends on the old stage callback contract.
- **Required change:** Remove this obsolete `RenderHighlightEvent.Block` subscription and call. The owned `BlockHighlightRenderer` now subscribes to `ExtractBlockOutlineRenderStateEvent`, snapshots the selected tool/cover/pipe geometry and texture IDs, and adds a target `CustomBlockOutlineRenderer`; keep the vanilla block outline additive by not canceling it. Do not re-add the old renderer call, which would duplicate the custom overlays.
- **Evidence:** Exact NeoForge 26.2.0.88 `ExtractBlockOutlineRenderStateEvent` provides target level/position/state/hit data and accepts custom outline renderers. `CustomBlockOutlineRenderer` requires extracted data only and receives `SubmitNodeCollector` at submission. Patched MC `LevelExtractor` stores these callbacks in `BlockOutlineRenderState`; `LevelRenderer.submitBlockOutline` invokes them before vanilla outline submission. The owned renderer captures only immutable vertices, UV/color values, and texture identifiers. The 26.2 sources no longer contain `RenderHighlightEvent.Block` or `MultiBufferSource`.
- **Blocks:** full compilation while the stale event/type and method call remain; the old listener currently fails independently on removed event APIs.
- **Owned dependency:** `client/renderer/BlockHighlightRenderer.java`.

### 8. Replace the legacy AABB-highlight stage call

- **File:** `src/main/java/com/gregtechceu/gtceu/client/ClientEventListener.java`, `onRenderLevelStageEvent`.
- **Current contract:** The shared stage listener calls `AABBHighlightRenderer.INSTANCE.tick(poseStack, bufferSource, camera)` after block entities. The owned renderer no longer has this immediate-render method because 26.2 no longer exposes the old stage/camera/buffer contract.
- **Required change:** Remove the AABB renderer import and the `AFTER_BLOCK_ENTITIES` call. `AABBHighlightRenderer` now extracts its timed highlight list through `ExtractLevelRenderStateEvent` and submits the same thick, blinking, camera-relative outline geometry through `SubmitCustomGeometryEvent`; it owns those subscriptions directly. Keep the `PatternPreviewRenderer` call until its independent migration is complete.
- **Evidence:** Patched Minecraft 26.2.0.88 `LevelRenderState` provides frame-local render data through `ContextKey`; NeoForge 26.2.0.88 `ExtractLevelRenderStateEvent`/`SubmitCustomGeometryEvent` expose extraction and deferred geometry submission. `GTRenderTypes.blockHighlightQuads()` keeps the prior no-depth, position-color overlay pipeline, and the new `PoseStack.Pose` overload in `RenderBufferHelper` emits the same 12 thick AABB edges.
- **Blocks:** full compilation while the stale `tick` call remains; the shared listener also has numerous unrelated 1.20 stage-event diagnostics.
- **Owned dependency:** `client/renderer/AABBHighlightRenderer.java` and `client/util/RenderBufferHelper.java`.

### 9. Remove the legacy pattern-preview stage call

- **File:** `src/main/java/com/gregtechceu/gtceu/client/ClientEventListener.java`, `onRenderLevelStageEvent`.
- **Current contract:** The shared stage listener invokes `PatternPreviewRenderer.INSTANCE.draw(...)` with the removed `RenderLevelStageEvent.Stage`, camera, and `MultiBufferSource.BufferSource` API. The owned renderer no longer exposes this method and now subscribes to target extraction/submission events itself.
- **Required change:** Remove the obsolete `onRenderLevelStageEvent` method once request 8's AABB call is removed; this method contains only the two migrated preview/highlight dispatches. Keep the separate `PatternPreviewRenderer.clientTick()` lifecycle call and all unrelated listener methods.
- **Evidence:** NeoForge 26.2.0.88 `ExtractLevelRenderStateEvent` runs after vanilla extraction and supplies the active `ClientLevel`, frustum, delta tracker, and `LevelRenderState`; `SubmitCustomGeometryEvent` supplies that state and the collector. Patched MC `BlockModelRenderState` and NeoForge `submitMultiLayerBlockModel` carry contextual model parts, tint layers, render flags, and translucent ordering; `BlockEntityRenderDispatcher` extracts per-frame immutable render states. The owned preview now uses these APIs and has zero diagnostics in the integrated compile.
- **Blocks:** full compilation while the stale `draw` call and removed stage/buffer types remain in the shared listener.
- **Owned dependency:** `client/renderer/PatternPreviewRenderer.java`.

### 10. Emit target dynamic pipe block-state models

- **Files:** `src/main/java/com/gregtechceu/gtceu/client/ClientProxy.java#onRegisterModelLoaders`, `src/main/java/com/gregtechceu/gtceu/data/model/builder/PipeModelBuilder.java#toJson`, `src/main/java/com/gregtechceu/gtceu/client/model/pipe/PipeModel.java#getOrCreateBlockModel`, and `src/main/java/com/gregtechceu/gtceu/api/pipenet/IPipeNode.java#getModelData`.
- **Current contract:** `ClientProxy` registers `PipeModelLoader` as a legacy `IGeometryLoader`. The generated `PipeModelBuilder#toJson` document uses the old `{"loader":"gtceu:pipe", ...}` shape. `IPipeNode#getModelData()` already snapshots connection and blocked masks, but also writes LEVEL from a statically typed `Level` into `ModelProperty<BlockAndTintGetter>`, which is a current compile error. Legacy `BakedPipeModel` reads live block entities for covers and frame material.
- **Required change:** Remove `event.register(PipeModelLoader.ID, PipeModelLoader.INSTANCE)` from `ClientProxy#onRegisterModelLoaders`; the owned `PipeBlockStateModelType#register(RegisterBlockStateModels)` subscriber registers the target codec. Update `PipeModelBuilder#toJson` to emit the target custom model type and `parts`/`restrictors` maps of 26.2 `BlockStateModel.Unbaked` definitions. Preserve the center, all six directional connectors, restrictors, rotations, UV lock, and weighted child models. Keep the separate generated item model path.

- In `IPipeNode#getModelData()`, store `self().getLevel()` in a local typed as `BlockAndTintGetter` before passing it to `GTModelProperties.LEVEL`; keep the real level, position, and current mask snapshots while legacy consumers remain. Add `PIPE_FRAME_STATE`, `COVER_MODEL_DATA`, and `FACADE_RENDER_STATE`; store `PIPE_PAINTING_COLOR` only when `isPainted()` (leave it absent otherwise). The new owned model reads all values through the passed `level.getModelData(pos)`; it does not look up a live pipe node.
- **Owned implementation:** `PipeBlockStateModelType#register(RegisterBlockStateModels)` registers the target codec; `UnbakedPipeBlockStateModel` resolves and bakes its children; `PipeBlockStateModel` collects center/connection/restrictor parts from the worker snapshot. It resolves `PIPE_FRAME_STATE` through the active 26.2 model set and collects it with its actual state, skips frame cull faces behind copied covers, bakes a copied paint color into pipe-child tint layers 0/1 while clearing their tint indices, and now composites facade planes/back plates from `FACADE_RENDER_STATE`. Unpainted child and frame colors remain on the target block-tint-source path. The geometry key includes masks, frame, paint, covered sides, selected child keys, and facade geometry. Generic non-facade cover geometry and shared snapshot production remain open; generated JSON must not switch until those paths are wired.
- **Evidence:** NeoForge 26.2.0.88 `RegisterBlockStateModels`, `CustomUnbakedBlockStateModel`, `DynamicBlockStateModel`, and worker-snapshot javadocs; patched MC 26.2.0.88 `BlockStateModel`/`BlockStateModelPart`/`BlockStateModelSet`. Integrated compile `build/friend-client-port-machine-pipe-facade-composition-verify.log`: 873 global / 534 client-path / 17 client-mixin-path diagnostics. The new pipe and machine/facade model classes have no diagnostics; old loader/baked/generator classes remain in the compiler error set. The compiler still reports the exact `IPipeNode.java:128` generic mismatch above.
- **Blocks:** full connected pipe compilation, dynamic resource data generation, paint/frame/cover visuals, and runtime reload/appearance verification.
- **Owned dependency:** `client/model/GTModelProperties.java`, `client/model/pipe/PipeBlockStateModelType.java`, `PipeBlockStateModel.java`, and `UnbakedPipeBlockStateModel.java`. Shared callers/data generation and the `IPipeNode` producer remain outside ownership.

### 11. Replace the legacy lamp item renderer hookup

- **Files:** `src/main/java/com/gregtechceu/gtceu/common/item/LampBlockItem.java#initializeClient` and `src/main/java/com/gregtechceu/gtceu/common/data/GTBlocks.java` lamp/borderless-lamp item model registrations.
- **Current contract:** `LampBlockItem.initializeClient` returns the removed `BlockEntityWithoutLevelRenderer` through `IClientItemExtensions.getCustomRenderer`. `GTBlocks` generates lamp item models with the legacy `CustomItemRendererWrapperModel` loader. `LampItemRenderer` reads `ItemStackData`, selects among eight lamp block states, renders each block-model pass, and applies the item foil buffer.
- **Required change:** Replace the old hook and generated item model with the target special-item-model path: register a `SpecialModelRenderer.Unbaked` codec on `RegisterSpecialModelRendererEvent`, and use a `minecraft:special`/`ItemModelUtils.specialModel` definition for both lamp families. Keep all eight `ItemStackData` state combinations, the base item model's transforms/tints, and enchanted foil rendering.
- **Evidence:** Exact patched Minecraft 26.2.0.88 sources define `SpecialModelRenderer`, `SpecialModelWrapper`, and `ItemModelUtils.specialModel`; NeoForge 26.2.0.88 defines `RegisterSpecialModelRendererEvent` and `IClientItemExtensions` without `getCustomRenderer`. The old owned `LampItemRenderer` diagnostics are confined to removed custom-renderer/model APIs.
- **Blocks:** current full compilation through the common `LampBlockItem` hookup and lamp item rendering/variant visuals.
- **Owned dependency:** `client/renderer/item/LampItemRenderer.java`.

### 12. Provide a target machine model and block-entity renderer bridge

- **Files:** `src/main/java/com/gregtechceu/gtceu/client/ClientProxy.java#onRegisterModelLoaders`; the shared machine model/renderer registration path; `src/main/java/com/gregtechceu/gtceu/api/machine/MetaMachine.java#getRenderBoundingBox`.
- **Current contract:** `ClientProxy` registers `MachineModelLoader` as a legacy `IGeometryLoader`. It produces `MachineModel`, which combines baked machine-state variants, multipart selectors, covers, and dynamic renders in one legacy `BakedModel`/`BlockEntityRenderer` object. `MetaMachine.getRenderBoundingBox` retrieves the baked model for its block state and expects it to implement `IBlockEntityRendererBakedModel`.
- **Required change:** Remove the old `event.register(MachineModelLoader.ID, MachineModelLoader.INSTANCE)` call from `ClientProxy#onRegisterModelLoaders`; the owned `MachineBlockStateModelType#register(RegisterBlockStateModels)` subscriber registers the target codec. Update `MachineModelBuilder#toJson` and its `GTMachineModels` caller to emit the target `type` model schema and 26.2 `BlockStateModel.Unbaked` child definitions. The current builder serializes some nested `BlockModelBuilder` values inline; move those to model resources and reference their identifiers so the target variant codec can resolve them. Update `MetaMachine#getModelData()` to copy `GTModelProperties.MACHINE_RENDER_STATE` plus immutable cover/output inputs into the block entity's `ModelData`; the meshing worker reads that snapshot through the passed level's `getModelData(pos)`. Then move dynamic machine, cover, monitor, placeholder, and Fusion Reactor bloom rendering to a registered target `BlockEntityRenderer` with extracted immutable state and `SubmitNodeCollector` submission. Preserve monitor/image quads and text, cover-face orientation, Fusion Reactor visibility/fade, and distinct light-ring/bloom outputs. Replace `MetaMachine#getRenderBoundingBox()` baked-model lookup with the target renderer's bounds extension and provide the machine-item rendering route separately. Do not query a live `MetaMachine` from chunk-meshing workers or silently remove these render features.
- **Owned implementation:** `MachineBlockStateModelType#register(RegisterBlockStateModels)` registers the target codec; `UnbakedMachineBlockStateModel` resolves target child dependencies and bakes weighted variants/multipart selectors; `MachineBlockStateModel` selects geometry from `GTModelProperties.MACHINE_RENDER_STATE` on the model-data snapshot, with a default-state fallback. It composes facade and ordinary cover models with their snapshot-derived keys and material flags. The target bake resolves dynamic descriptors, replaceable texture keys, and baked texture overrides. `DynamicRender` exposes target extraction/submission independently of the removed immediate-buffer `IMachineRendererModel` contract; target BER consumers use it directly. `MachineBlockStateModel` maps old formed-part texture slots and applies overrides only to formed-part target quads. `MachineBlockEntityRenderer` implements target create/extract/submit; owned snapshots cover machine effects, cover text, built-in monitor modules, and dynamic machine item contents. Shared BER/model-data/asset wiring remains pending, so do not switch generated models yet.
- **Required BER registration:** In `ClientProxy#onRegisterEntityRenderers(EntityRenderersEvent.RegisterRenderers)`, iterate the finalized `GTRegistries.MACHINES` definitions, identity-dedupe their `MachineDefinition#getBlockEntityType()` values, and register each with `event.registerBlockEntityRenderer(type, MachineBlockEntityRenderer::new)`. Registration must run after each definition's type supplier is populated. The renderer re-resolves the active model during extraction/submission, checks the definition identity, and uses `MachineBlockStateModel.find()` to unwrap CTM wrappers; its snapshot carries only the definition, block state, and descriptor-indexed immutable payloads.
- **Evidence:** Patched Minecraft 26.2.0.88 `BlockStateModel`/`BlockStateModelPart` collect target parts; `ModelBaker.materials().get(Material, ModelDebugName)` resolves target block-atlas sprites; NeoForge 26.2.0.88 `CustomUnbakedBlockStateModel`, `RegisterBlockStateModels`, `DynamicBlockStateModel`, and `BlockStateModelExtension` define the codec/worker contract. The extension explicitly says contextual collection runs on meshing workers and block entity data must come from the passed level's model-data snapshot. Latest integrated compile `build/friend-client-port-machine-formed-part-textures-verify.log` reports 877 global / 530 exact-owned errors; new target machine and pipe classes have no diagnostics, while legacy loader/model classes remain in the error set. `MetaMachine#getRenderBoundingBox()` still looks up a legacy `BakedModel`; dynamic callbacks still expose removed buffer APIs.
- **Monitor owned follow-up:** `CentralMonitorRender` extracts the legacy slot-0 modules into `MonitorRenderSnapshot`; `MonitorTextRenderer`/`MonitorImageRenderer` snapshot built-in text and image outputs, and text graphics support built-in `rect`/`quad` plus nested module renderers that implement `IMonitorRenderSnapshotProvider`. `MonitorGuiRenderer` moves active hit testing and click forwarding to extraction. Verification `build/friend-client-port-monitor-snapshot-verify4.log` is 873 total errors (534 client-path / 17 client-mixin-path); only the still-required legacy callback declarations report errors in these monitor classes.
- **Required placeholder API change:** In `src/main/java/com/gregtechceu/gtceu/api/placeholder/IPlaceholderRenderer.java` and `PlaceholderHandler`, add a target extraction hook/registration that returns an immutable client snapshot provider for `renderData`; do not wrap all providers as an opaque `IMonitorRenderer` lambda. Keep existing `rendererId`, copied NBT data, text ordering, and per-graphic x/y/depth transform. Implementations must not retain `CentralMonitorMachine`, `MonitorGroup`, `Level`, or mutable NBT in deferred submission. The current owned path explicitly handles the built-in `rect`, `quad`, and nested `module` IDs; unknown/custom IDs require their provider and are skipped until it exists.
- **Required GUI module hookup:** The common `GuiModuleBehaviour#getRenderer` currently returns `MonitorGuiRenderer`, whose visible old GUI drawing path is commented out; its active behavior is hit testing and click forwarding, now extracted by the owned provider. When migrating the common `IMonitorRenderer` route, invoke its extraction hook once per frame and keep the GUI input dispatch on the client thread. A replacement for any restored in-world GUI texture must use a separately extracted ModularUI render target/image snapshot.
- **Blocks:** compiling/wiring the full machine/multipart renderer path, correct machine/item/cover/monitor/fusion visuals, and runtime validation. Shared edits are separate from the owned implementation and are not yet applied.
- **Owned dependency:** `client/model/machine/MachineModel.java`, `UnbakedMachineModel.java`, `MachineModelLoader.java`, `client/model/machine/multipart/**`, `client/renderer/BlockEntityWithBERModelRenderer.java`, `client/renderer/cover/IDynamicCoverRenderer.java`, `client/renderer/monitor/IMonitorRenderer.java`, `client/renderer/placeholder/QuadPlaceholderRenderer.java`, and `client/renderer/machine/impl/FusionRingRender.java`. The shared placeholder callback in `api/placeholder/IPlaceholderRenderer.java` and Fusion Reactor machine ticket field remain outside this assignment.

- **Follow-up evidence:** `UnbakedMachineBlockStateModel.resolveDependencies` now marks each `texture_overrides` identifier through patched 26.2 `ResolvableModel.Resolver.markDependency`, ensuring replacement sprites are included before the target atlas bake. Integrated offline compile `build/friend-client-port-machine-texture-dependencies-verify.log` reports **878 global / 530 exact-owned diagnostics**; the target machine, pipe, and facade model classes have no diagnostics. Dynamic BER submission and shared model-data production remain open.

- **Follow-up owned implementation:** `MachineOutputRenderState` contains item/fluid output directions and their auto-output flags under `GTModelProperties.MACHINE_OUTPUT_RENDER_STATE`. `MachineBlockStateModel` emits the old pipe overlay on each configured face, the matching fluid/item auto overlay when enabled, adds the snapshot to its geometry key, and reports overlay material flags. `UnbakedMachineBlockStateModel` registers and bakes the three overlay sprite dependencies. The shared producer should publish a direction only when its `AutoOutputTrait` supports that output; otherwise leave it null. Verification `build/friend-client-port-machine-output-overlays-verify.log`: **878 global / 530 exact-owned diagnostics**; the new target machine model classes have no diagnostics. Producer wiring and runtime visuals remain pending.

- **Follow-up owned implementation:** target machine CTM probing scans baked variant and multipart child models via `CTMModelPartSource`, so `ModelEventHelper` can wrap a dynamic machine model when a non-default child contains connected-texture sprites. Contextual part selection is unchanged. The traversal samples nested weighted child models using the deterministic CTM probe seed and does not claim exhaustive coverage of every weighted entry. Integrated offline compile `build/friend-client-port-machine-ctm-candidates-verify.log`: **878 global / 530 exact-owned diagnostics**; `MachineBlockStateModel`, `ModelEventHelper`, and the new candidate-source interface have no diagnostics. No tests or runtime visuals ran.

- **Follow-up owned implementation:** `MachineBlockEntityRenderer` implements patched 26.2 `BlockEntityRenderer<MetaMachine, MachineRenderSnapshot>` lifecycle methods and re-resolves the active target model at submission. `MachineRenderSnapshot` carries a stable machine definition, block state, and indexed immutable descriptor payloads. `DynamicRender` adds extraction/submission hooks, and `AssemblyLineRender` preserves laser geometry through a captured progress/orientation/color snapshot and `SubmitNodeCollector.submitCustomGeometry`. Patched MC 26.2 `BlockEntityRenderer.java` requires `createRenderState`, `extractRenderState`, and `submit`; NeoForge `IBlockEntityRendererExtension` supplies entity-specific bounds, while `shouldRenderOffScreen()` has no entity parameter. This bridge returns true for that renderer-wide flag and filters per-machine descriptors in `shouldRender`, so check global BER cost after registration. Integrated offline compile `build/friend-client-port-machine-ber-assembly-line-verify2.log`: **878 global / 530 exact-owned diagnostics**; the bridge/snapshot classes have no diagnostics. The legacy callback remains during the transition, and other dynamic descriptors/covers have not yet implemented target snapshots. Do not register/switch generated machine models until those paths are migrated. No tests or runtime visuals ran.

  - **Follow-up owned implementation:** `FluidAreaRender` extracts an immutable snapshot containing the chosen fluid, target `FluidModel` transparency layer, resolved machine-relative plane directions, copied trait offsets, and per-offset packed light values. `FluidBlockRenderer` has a pose-based helper that consumes these values without retaining a level. The shared machine renderer must include `FluidAreaRender` in its descriptor extraction/submission set; retain the old callback until the common caller is migrated. Integrated offline compile `build/friend-client-port-fluid-area-snapshot-verify3.log` reports **877 global errors** (538 diagnostics in the `client/` source path and 17 in `core/mixins/client/`); the new target extraction/submission and helper methods add no diagnostics. No runtime visuals ran.

  - **Follow-up owned implementation:** `QuantumTankFluidRender` extracts the selected fluid, stack tint through the 26.2 `FluidModel.fluidTintSource().colorAsStack`, stored amount/capacity, facing, gas state, and creative flag. Its deferred callback submits the volume using the block atlas and amount text with the captured front orientation; no live machine or fluid stack reaches submission. `QuantumChestItemRender` supplies the pose-based amount-label helper. Shared BER registration must include the descriptor; its item-stack callback still needs a separate target item-model implementation. Verification `build/friend-client-port-quantum-tank-snapshot-verify2.log`: **877 global errors** (538 `client/`, 17 `core/mixins/client/`); new extraction/submission/helper methods add no diagnostics. No tests or visuals ran.

  - **Follow-up owned implementation:** `FusionRingRender` captures and submits the animated ring from immutable fade/color/axis state, preserving the current active glow, idle fade, and pulsing geometry. `GTRenderTypes.bloomLightRing()` adds a position-color target pipeline and routes it directly to the managed bloom source while bloom is active. Its duplicate per-machine ticket callback has been removed; the shared machine BER should use this snapshot path. The generic `BloomHandler` ticket path now submits with a caller-selected 26.2 `RenderType` during `SubmitCustomGeometryEvent`. Integrated compile `build/friend-client-port-bloom-ticket-submit-verify.log`: **816 global / 468 exact-owned errors**, with no diagnostics in `BloomHandler`, `IRenderSetup`, or `FusionRingRender`. This does not validate event registration or rendered output. No tests or runtime visuals ran.

  - **Follow-up owned implementation:** `QuantumChestItemRender` resolves a copied stored/locked item stack through `ItemModelResolver` to `ItemStackRenderState` during extraction; submit receives only that frame render state, rotation/facing, amount, and creative flag. It preserves fixed-context model layers/foil, rotation timing, and the amount label. The machine BER should include the descriptor. The separate standalone item model still needs a generated `minecraft:special` definition and `SpecialModelRenderer` association from the shared item/model-data owner. Integrated compile `build/friend-client-port-quantum-chest-item-state-verify.log`: **877 global errors** (538 `client/`, 17 `core/mixins/client/`); target extraction/submission methods add no diagnostics. No runtime visual ran.

  - **Follow-up owned implementation:** formed boiler child geometry is provided by `ControllerPartModel` and `ControllerPartRenderState`, which are consumed by `MachineBlockStateModel` without live controller access from chunk workers. In `api/machine/MetaMachine.java#getModelData()`, publish `GTModelProperties.FORMED_PART_RENDER_STATE` only for a formed `MultiblockPartMachine` whose `replacePartModelWhenFormed()` is true. Copy the controller `BlockState`, immutable position, front/up directions, flipped flag, and active flag; clear the property when unformed or controllerless and refresh it on formation, controller, orientation, and active-state changes. Do not retain a controller or machine in ModelData. `BoilerMultiPartRender` implements the owned child-model contract; integrated compile `build/friend-client-port-formed-boiler-parts-verify2.log` reports 878 global / 530 exact-owned errors and no diagnostics in the new target part-model classes. No game visuals ran.

  - **Follow-up owned implementation:** `MachineBlockEntityRenderer.extractRenderState` visits each cover side, resolves `IDynamicCoverRenderer` only while the live machine is available, and stores the returned `DynamicCoverRenderSnapshot`; submit consumes only that immutable payload. `CoverTextRenderer` captures formatted component lines and submits them after applying the previous face placement, scale, full-bright light, and green color. `api/cover/CoverBehavior#getDynamicRenderer()` currently supplies this text renderer. Keep the machine BER as the snapshot owner and do not pass the cover behavior or supplier into frame state. Verification `build/friend-client-port-dynamic-cover-text-verify.log`: **877 global errors** (538 `client/`, 17 `core/mixins/client/`); new cover extraction/submission adds no diagnostics, though old callback lines still do. No tests or visuals ran.

  - **Follow-up owned implementation:** `DynamicRender` now owns the target descriptor visibility/bounds and immutable extraction/submission contract without inheriting the removed immediate-buffer `IMachineRendererModel` API. Assembly-line, fusion-ring, fluid-area, growing-plant, quantum-chest, and quantum-tank output now uses those target hooks; the boiler’s old `BakedModel` part renderer is replaced by its existing `ControllerPartModel` collector. Their obsolete immediate draw duplicates were removed only where target snapshot/part implementations now carry that output. Verification `build/friend-client-port-dynamic-snapshot-contract-verify.log`: **780 global / 432 exact-owned diagnostics**, down 29 / 36 from the preceding 809 / 461 compile; none of the changed target descriptors add diagnostics. The legacy `MachineModel`/`UnbakedMachineModel` and `ClientProxy` path still need the request 12 model/BER switch. No tests or visuals ran.

- **Facade composition follow-up:** Every target machine model now bakes a `FacadeBlockStateModel` with the cover-back-plate sprite dependency and appends its contextual facade parts; the façade geometry key and material flags are included in the host machine model. Verification `build/friend-client-port-machine-facade-composition-verify.log`: **873 global errors** (534 client-path / 17 client-mixin-path), with no diagnostics in `MachineBlockStateModel`, `UnbakedMachineBlockStateModel`, or `FacadeBlockStateModel`.
- **Required model-data producer:** In `src/main/java/com/gregtechceu/gtceu/api/machine/MetaMachine.java#getModelData`, publish `GTModelProperties.FACADE_RENDER_STATE` (facade states, plate/back-side flags, occupied faces, plate thickness), `GTModelProperties.COVER_RENDER_STATE`, and `GTModelProperties.COVER_MODEL_DATA` (the six child snapshots) into the host model data and invalidate it on cover add/remove/configuration changes. `MachineBlockStateModel` feeds those copied properties to the separate facade and ordinary-cover target models during worker collection. The owned machine/pipe cover-model integration is now implemented; request 22 records the separate `IPipeNode#getModelData()` publication and atlas directory source still needed. Do not access a live `CoverBehavior` or machine from the chunk worker.

### 13. Replace the removed named bloom render-type contract

- **Files:** any shared resource/datagen path or public model-definition compatibility layer that exposes the `gtceu:bloom` named render-type alias. The old `BloomEventListeners.ModBus.registerNamedRenderTypes` method has been removed from the owned source.
- **Current contract:** The target 26.2 API has no named-render-type registration event or equivalent alias registry. Repository search found no in-tree block/item model JSON or Java generator referring to the `bloom` alias. In-tree PNG metadata-driven block bloom now uses the owned `BloomChunkGeometry` section snapshot path; dynamic machine/particle bloom uses `GTRenderTypes` and collector submission. External packs or addons that relied on the public alias have not been checked.
- **Required change:** Confirm whether `gtceu:bloom` is a supported external resource-pack contract. If it is, define a target-version model/resource route for block and item definitions with the same active bloom and safe fallback behavior; do not reintroduce removed 1.20 named render types. Preserve the current texture-metadata bloom path.
- **Evidence:** Patched Minecraft 26.2.0.88 and NeoForge 26.2.0.88 sources contain `RegisterRenderPipelinesEvent`, `BlockStateModelPart`, `BlockModelRenderState`, and `AddSectionGeometryEvent`, but no `RegisterNamedRenderTypesEvent`. `ChunkSectionLayer` remains fixed; the new owned section capture submits metadata-selected geometry through `SubmitCustomGeometryEvent` instead of routing static blocks to a removed custom chunk layer. Compile `build/friend-client-port-bloom-section-geometry-verify2.log` adds no diagnostic in the new block bloom route. Repository search returned only bloom post-chain shader IDs, not an in-tree named model reference.
- **Blocks:** compatibility for external model definitions using the old named alias; no in-tree resource use was found. No runtime or external-pack validation ran.
- **Owned dependency:** `client/bloom/BloomChunkGeometry.java`, `client/bloom/BloomEventListeners.java`, and `client/renderer/GTRenderTypes.java`.

### 14. Remove the obsolete LevelRenderer mixin registration

- **File:** `src/main/resources/gtceu.mixins.json`, entry `client.LevelRendererMixin`.
- **Current contract:** The owned `LevelRendererMixin` wrapped the 1.20 `LevelRenderer.renderHitOutline` call and drew colored material/tier/wire outlines plus AoE block outlines. That target method no longer exists in 26.2.
- **Required change:** Remove the `client.LevelRendererMixin` entry. AoE block-breaking stages now append immutable `BlockBreakingRenderState` snapshots from `ExtractLevelRenderStateEvent`; colored material/tier/wire and AoE outlines now use `ExtractBlockOutlineRenderStateEvent` and `CustomBlockOutlineRenderer`. Keep the independent bloom `client.bloom.LevelRendererMixin` entry.
- **Evidence:** Patched Minecraft 26.2.0.88 `LevelRenderer` has `submitBlockOutline` and `submitBlockDestroyAnimation`, not `renderHitOutline`; its outline path invokes `BlockOutlineRenderState.customRenderers()` before vanilla submission, and its break path submits `LevelRenderState.blockBreakingRenderStates`. NeoForge 26.2.0.88 event sources confirm both extraction contracts. The replacement renderers hold copied positions/shapes/colors/progress and no live level/entity references.
- **Blocks:** client startup/mixin application while the obsolete `renderHitOutline` injector remains; Java compilation is unaffected.
- **Owned dependency:** `client/renderer/ColoredAoEBlockOutlineRenderer.java`, `client/renderer/AoEBlockBreakingRenderer.java`, and removal of `core/mixins/client/LevelRendererMixin.java`.

### 15. Resolve the pinned 1.20.1 Embeddium bloom integration

- **Files:** `gradle/forge.versions.toml` (`embeddium` version and module), plus the shared optional-integration/mixin registration if no 26.2-compatible Embeddium build exists.
- **Current contract:** The project targets Minecraft/NeoForge 26.2 but compiles the bloom mixins against `org.embeddedt:embeddium-1.20.1:0.3.31-beta.53+mc1.20.1`. Both `client.bloom.normal.embeddium.BlockRendererMixin` and `client.bloom.safemode.embeddium.BlockRendererMixin` target the 1.20.1 `BlockRenderer` descriptor. Their `BakedQuadView` is an Embeddium interface, not a Minecraft 26.2 `BakedQuad`; `ModelQuadView` exposes geometry, sprite, light, and normals, but no target material-emission value.
- **Required change:** Supply a 26.2-compatible Embeddium API/artifact and update both mixins against its exact source and bytecode contracts, including a supported way to preserve per-quad bloom metadata and emission. If no such build is available, provide a compatible optional-integration implementation/registration plan that keeps bloom behavior and does not load 1.20.1 mixins into 26.2. Do not add an unchecked cast to `BakedQuad` or infer material emission from packed lighting.
- **Evidence:** `gradle/forge.versions.toml` pins the module above. Its only cached source jar is `embeddium-1.20.1-0.3.31-beta.53+mc1.20.1-sources.jar`; `BakedQuadView` extends `ModelQuadView`, which has no `materialInfo()` or emission accessor. The current exact owned diagnostics include invalid `BakedQuadView` to `BakedQuad` casts and removed 1.20 `VertexConsumer` calls. No 26.2 Embeddium artifact/source was found in the local Gradle cache.
- **Blocks:** compilation of both optional Embeddium bloom mixins; correct bloom behavior and mixin application whenever Embeddium is present.
- **Owned dependency:** `core/mixins/client/bloom/normal/embeddium/BlockRendererMixin.java` and `core/mixins/client/bloom/safemode/embeddium/BlockRendererMixin.java`.

### 16. Register the growing-stem fruit accessor

- **File:** `src/main/resources/gtceu.mixins.json`, client mixins list.
- **Current contract:** The new owned `client.StemBlockAccessorMixin` exposes `StemBlock`'s private `fruit` `ResourceKey<Block>` so recipe-progress previews can render the configured fruit after the stem reaches its mature stage. The shared mixin configuration does not yet list this accessor.
- **Required change:** Add `client.StemBlockAccessorMixin` to the client mixin list and retain the existing client mixin package/configuration conventions.
- **Evidence:** Patched Minecraft 26.2.0.88 `StemBlock` stores `fruit` as a private final `ResourceKey<Block>` used by its codec and provides no public getter. The owned preview resolves that key from the block registry and returns the fruit's default state. The accessor and preview compile without diagnostics other than the preview's unrelated removed `MultiBufferSource` callback signature.
- **Blocks:** Modded stem fruit preview behavior until the accessor is applied; Java compilation is unaffected.
- **Owned dependency:** `client/renderer/machine/impl/GrowingPlantRender.java` and `core/mixins/client/StemBlockAccessorMixin.java`.

### 17. Publish facade-cover snapshots to the target machine model

- **Files/methods:** The shared owner’s `src/main/java/com/gregtechceu/gtceu/api/machine/MetaMachine.java#getModelData()` (or the concrete machine model-data producer), and `src/main/java/com/gregtechceu/gtceu/data/model/builder/MachineModelBuilder#toJson()` plus its generated machine block-state model definition.
- **Current contract:** The owned `FacadeBlockStateModel` and `FacadeRenderState` now implement 26.2 cover geometry. The model reads `GTModelProperties.FACADE_RENDER_STATE` containing one immutable entry per attachment side (facade `BlockState`, `shouldRenderPlate`, and holder `shouldRenderBackSide`), plus the existing side-indexed `GTModelProperties.COVER_MODEL_DATA` child snapshots. Its custom codec is registered as `gtceu:facade` through the owned `FacadeBlockStateModelType` subscriber. No shared producer currently publishes the new facade property or guarantees the facade model is included in the target machine model.
- **Required change:** In `MetaMachine#getModelData()`, copy facade cover state and both old filtering flags into a `FacadeRenderState` and put it under `FACADE_RENDER_STATE`; continue to publish each cover renderer’s child ModelData under `COVER_MODEL_DATA`. In `MachineModelBuilder#toJson()`, emit the registered custom machine model and add an unconditional multipart child `{ "type": "gtceu:facade" }` using the machine codec’s `multipart[].apply` field. Do not capture a live `CoverBehavior`, host block entity, world, or baked model in the snapshot. Keep the normal facade item model separate from this block cover path.
- **Behavior/lifecycle:** The worker resolves facade models from the active `ModelManager` model set, applies the copied side/plate/back-side selection, clips quads to the same inset face planes, re-interpolates UVs, and evaluates block tint sources with the real facade state and host world position. The model retains no model-manager child reference over reload.
- **Evidence/verification:** Patched Minecraft 26.2.0.88 sources provide contextual `BlockStateModelSet`, `BlockTintSource#colorInWorld`, and `BlockStateModelPart`; the active integrated compile log `build/friend-client-port-facade-model-verify2.log` is 891 global / 544 owned errors, unchanged from the resume baseline. All newly added target facade model classes have zero diagnostics. No game launch or visual verification has run.
- **Blocks/impact:** Until the producer and generated machine model are updated, no actual machine displays the new facade model. This does not yet replace `FacadeUnbakedModel` or `FacadeCoverRenderer`’s legacy standalone item model behavior; that target item route remains an owned task.
- **Owned implementation:** `client/model/item/FacadeRenderState.java`, `FacadeBlockStateModel.java`, `UnbakedFacadeBlockStateModel.java`, `FacadeBlockStateModelType.java`, and `client/model/GTModelProperties.java`.
- **Follow-up owned work:** `FacadeRenderState` now also carries `coverPlateThickness` and `occupiedFaces`; `FacadeBlockStateModel` emits the legacy backplate back-face and exposed sides, while preserving the old inset dimensions and suppressing occupied sides and the attachment face. The shared producer must populate these fields from the host cover container. `UnbakedFacadeBlockStateModel` registers `block/cover/cover_back_plate` as an atlas dependency and bakes a reload-scoped sprite. Integrated offline compile `build/friend-client-port-facade-backplate-verify2.log`: **878 global / 530 exact-owned diagnostics**; the target facade classes have no diagnostics. Full project compile, producer wiring, and visual checks remain pending.

- **Latest verification:** the facade model-level material flags now include the generated backplate quads. Compile `build/friend-client-port-facade-backplate-verify3.log` remains **878 global / 530 exact-owned diagnostics**; the target facade model classes have zero diagnostics. No runtime/visual checks were run.

### 18. Route lamp items through the registered special renderer

- **Files/methods:** `src/main/java/com/gregtechceu/gtceu/common/item/LampBlockItem.java#initializeClient()`, `src/main/java/com/gregtechceu/gtceu/common/data/GTBlocks.java` lamp and borderless-lamp item model builders, and `src/main/java/com/gregtechceu/gtceu/client/ClientProxy.java#onRegisterModelLoaders()`.
- **Current contract:** The owned `LampItemRenderer` now implements `SpecialModelRenderer<BlockState>` and extracts the stack-specific `LampBlockItem#getStateFromStack` result. It collects that state’s target block model, resolves 26.2 block tint sources, submits its parts through `SubmitNodeCollector`, and emits foil with the target glint render type. `LampItemRendererType` registers its `gtceu:lamp` special-renderer codec on `RegisterSpecialModelRendererEvent`. The old `IClientItemExtensions#getCustomRenderer` hook no longer exists in NeoForge 26.2.
- **Required change:** Remove `LampBlockItem#initializeClient()` and its `BlockEntityWithoutLevelRenderer`/`IClientItemExtensions` registration. Update both `GTBlocks` lamp item model builders to generate the target client item definition using `minecraft:special`, retain each existing `_on` block-item model as the `base` (for its GUI/hand transforms), and set its special model to `{ "type": "gtceu:lamp" }`. Once no generated JSON uses the old loader, remove its `CustomItemRendererWrapperModel` registration from `ClientProxy#onRegisterModelLoaders()`; retain unrelated registrations in that method.
- **Behavior/lifecycle:** The lamp item keeps its NBT/component-selected inverted, bloom, and light flags through `getStateFromStack`. The target item wrapper uses the base model’s display transform and passes foil/outline state to the special renderer. The renderer stores no stack or model reference between submissions and re-queries the active model set after reload.
- **Evidence/verification:** Patched MC 26.2 `SpecialModelRenderer`, `SpecialModelWrapper`, `BlockStateModelSet`, and `BlockModelRenderState`, and NeoForge `RegisterSpecialModelRendererEvent` confirm this route. `build/friend-client-port-lamp-special-item-verify.log` reaches javac and reports 878 global / 530 exact-owned errors, down 13 / 14 from the resume baseline. The new special-renderer classes have zero diagnostics; the remaining `LampBlockItem#initializeClient()` errors are shared-side wiring. No game launch or item visuals ran.
- **Blocks/impact:** The new renderer is not selected until the generated lamp client-item definition is changed. Current full-project build remains blocked by the recorded unrelated errors.
- **Owned implementation:** `client/renderer/item/LampItemRenderer.java` and `LampItemRendererType.java`.
### 19. Point the facade cover item at its target item model

- **Files:** `src/main/resources/assets/gtceu/models/item/facade_cover.json`, a new `models/item/facade_cover_base.json`, a new `assets/gtceu/items/facade_cover.json`, and `src/main/java/com/gregtechceu/gtceu/client/ClientProxy.java#onRegisterModelLoaders()`.
- **Current contract:** The owned `FacadeItemModel` implements the target `ItemModel` path and registers its `gtceu:facade` codec with `RegisterItemModelsEvent`. Its unbaked `base` identifier resolves a target item geometry model through `ModelBaker`; runtime selection still uses `FacadeItemBehaviour#getFacadeStateNullable(stack)`. It preserves the old plate geometry/transform route and clips/tints the north-facing facade block model.
- **Required change:** Move the existing nested `default_model` object in `models/item/facade_cover.json` unchanged to `models/item/facade_cover_base.json`, preserving its texture, element faces, and display transforms. Add the client item definition `{ "model": { "type": "gtceu:facade", "base": "gtceu:item/facade_cover_base" } }`. Once the old `loader: gtceu:facade` model is no longer referenced, remove the `FacadeUnbakedModel.Loader` registration from `ClientProxy#onRegisterModelLoaders()` and retain unrelated registrations.
- **Behavior/lifecycle:** The base geometry, item transforms/light/particle properties, base item tint layers, item foil, and state-selected facade north-face quads use the target item renderer. The facade overlay uses item-context block tint defaults and an empty block/model-data snapshot; custom facade appearances that require a block entity or position-specific ModelData remain unsupported/unverified.
- **Evidence/verification:** Patched MC 26.2 `ItemModel`, `ItemModel.Unbaked`, `CuboidItemModelWrapper`, `ModelRenderProperties`, `BlockStateModelSet`, and `BakedQuad`, plus NeoForge `RegisterItemModelsEvent`, provide these target contracts. `build/friend-client-port-facade-item-model-verify2.log` reports 878 global / 530 exact-owned errors, unchanged from the prior compiler pass. The new target facade item classes have zero diagnostics. No game launch or item visuals ran.
- **Blocks/impact:** Until the target client item definition and base asset are generated, the new facade item model is not selected; the legacy `FacadeUnbakedModel` loader still causes compiler diagnostics until the shared registration is removed.
- **Owned implementation:** `client/model/item/FacadeItemModel.java` and `FacadeItemModelType.java`.

### 20. Route dynamic machine-item content through the 26.2 special item model

- **Files:** `src/main/java/com/gregtechceu/gtceu/api/item/MetaMachineItem.java#initializeClient`, `src/main/java/com/gregtechceu/gtceu/api/registry/registrate/MachineBuilder.java#ItemBuilderWrapper.makeItemBuilder`, and the KubeJS machine-item model output in `MachineBuilder.KJSCallWrapper.generateAssetJsons`.
- **Current contract:** `MetaMachineItem#initializeClient` returns the removed `ItemWithBERModelRenderer` through `IClientItemExtensions#getCustomRenderer`. The normal item model is generated with a parent of `block/machine/<machine>`. The owned `MachineItemSpecialRendererType` registers `gtceu:machine_dynamic`, and `MachineItemSpecialRenderer` extracts snapshots only from dynamic machine renderers implementing `MachineItemRenderSnapshotProvider`; quantum chest/tank snapshots preserve the stored stack or fluid, transforms, text, amount, and animation data.
- **Required change:** For machine item definitions, preserve the existing `block/machine/<machine>` model as the `base` and emit an `assets/gtceu/items/<machine>.json` definition whose root model is `minecraft:special` with `model: { "type": "gtceu:machine_dynamic" }`. Apply the same representation to KubeJS-generated machine items. Remove the `MetaMachineItem#initializeClient` legacy BEWLR hook after the target item definitions are emitted; do not replace the base item model with a special renderer that loses its transform/material properties.
- **Behavior/lifecycle:** The target special wrapper derives item render properties from the base model and preserves foil. Machine items without a snapshot render only their base; supported dynamic contents are extracted into immutable frame-local item render states. The special renderer scans the active default-state `MachineBlockStateModel`, so target block-model codec registration must remain enabled.
- **Evidence/verification:** Patched MC 26.2 `SpecialModelWrapper.Unbaked` uses `base` to resolve `ModelRenderProperties` and decodes the nested special-renderer codec; `SpecialModelRenderer` extracts an item argument and submits via `SubmitNodeCollector`. NeoForge 26.2 `RegisterSpecialModelRendererEvent` registers the `gtceu:machine_dynamic` codec. `build/friend-client-port-local-item-snapshot-fix-verify.log` and `build/friend-client-port-local-fluid-tint-ticket-verify.log` show no diagnostics in the new machine item special-renderer classes; no runtime item check has run.
- **Blocks/impact:** Until both machine item model producers and the `MetaMachineItem` hook are updated, no machine item selects the new dynamic content path; existing generated model definitions continue to use the old custom-renderer contract.
- **Owned implementation:** `client/renderer/item/MachineItemSpecialRenderer.java`, `MachineItemSpecialRendererType.java`, `MachineItemRenderSnapshot.java`, and the quantum chest/tank snapshot providers.

### 21. Preserve active-pipe face emissivity in the project model builder

- **File:** `src/main/java/com/gregtechceu/gtceu/data/model/builder/ModelBuilder.java#FaceBuilder` (outside client-port ownership).
- **Current contract:** The client-owned `PipeModel` and `ActivablePipeModel` now use the repository’s `BlockModelBuilder`, `ModelBuilder`, `ModelFile`, and `VariantBlockStateBuilder` instead of removed Forge model-generator types. The custom `FaceBuilder` currently serializes texture, cull face, rotation, tint index, and UV only. `ActivablePipeModel` still calls `FaceBuilder.emissivity(activeEmissivity, activeEmissivity).ao(false)` for emissive active side/end faces; those two calls currently fail compilation because the custom builder exposes neither method.
- **Required change:** Add a behavior-preserving representation for per-face emissivity and ambient-occlusion metadata to the shared custom model-builder output, and ensure the target pipe-model path carries those values into the correct face quads/render submission. Preserve the configurable emissivity level and the existing side/end face selection; do not drop the two calls or silently make active pipe faces non-emissive.
- **Evidence/impact:** `build/friend-client-port-pipe-datagen-builder-verify2.log` reaches javac with 830 global / 483 exact-owned errors. `PipeModel` and `ActivablePipeModel` no longer report removed Forge builder APIs; only the two unsupported `FaceBuilder` methods remain in `ActivablePipeModel`. This blocks compilation of those active-pipe model calls and the generated emissive face behavior. The shared model-builder file is outside this assignment’s allowlist.
- **Owned dependency:** `client/model/pipe/ActivablePipeModel.java`.

### 22. Publish immutable ordinary-cover snapshots and include cover sprites in the block atlas

- **Files/methods:** `src/main/java/com/gregtechceu/gtceu/api/machine/MetaMachine.java#getModelData()`, `src/main/java/com/gregtechceu/gtceu/api/pipenet/IPipeNode.java#getModelData()`, and the shared 26.2 block-atlas source definition (the repository currently has no `assets/minecraft/atlases/blocks.json`).
- **Current contract:** Owned `CoverRenderState.capture(ICoverable)` copies the plate thickness, occupied-face mask, per-side plate flag, and the selected Simple/I/O overlay IDs and emissive behavior. `MachineBlockStateModel` and `PipeBlockStateModel` compose `CoverBlockStateModel`, whose per-baked-model cache emits ordinary cover overlay and backplate parts. Facades remain in `FacadeBlockStateModel` and are skipped by this generic snapshot so they are not rendered twice. The snapshots retain no live cover, holder, renderer, level, or baked sprite.
- **Required change:** Beside the existing `FACADE_RENDER_STATE` and `COVER_MODEL_DATA` publication, have the machine and pipe model-data producers build `CoverRenderState.capture(coverContainer)` and put it under `GTModelProperties.COVER_RENDER_STATE`. Ensure the producer is refreshed after cover install/removal/configuration and plate-thickness changes. Add a `minecraft:directory` block-atlas source with `source: "block/cover"` and `prefix: "block/cover/"` (or an equivalent reload-safe registration) so IDs under `gtceu:block/cover/` are available to the active atlas, including textures selected dynamically and optional `_emissive` companions. Do not store `CoverBehavior` or renderer instances in ModelData.
- **Behavior/lifecycle:** Preserve the old attached-face overlay, optional simple-cover emissive companion, forced full-bright I/O emissive layer and inverted-texture fallback, plate thickness, occupied-side suppression, and facade separation. The client model resolves sprites from the current block atlas and caches only within its baked model generation.
- **Evidence/impact:** The 26.2 patched `DirectoryLister` source confirms that atlas JSON uses `source` and `prefix`; project textures exist at `assets/gtceu/textures/block/cover/`, but no block-atlas definition exists in project resources. Until both publishers and atlas input are added, the generic model sees `EMPTY` model data or missing sprites. This shared/common and resource work is outside the client-port ownership boundary.
- **Owned implementation:** `client/model/CoverRenderState.java`, `CoverBlockStateModel.java`, `GTModelProperties.java`, and composition in `client/model/machine/MachineBlockStateModel.java` and `client/model/pipe/PipeBlockStateModel.java`.
### 23. Remove obsolete vanilla bloom mixin registrations

- **File:** `src/main/resources/gtceu.mixins.json`, client bloom mixin list.
- **Current contract:** The owned sources `client.bloom.ForgeModelBlockRendererMixin`, `client.bloom.LevelRendererAccessor`, `client.bloom.ModelBlockRendererMixin`, `client.bloom.QuadLighterMixin`, and `client.bloom.RebuildTaskMixin` have been deleted after static block bloom moved to the supported 26.2 `AddSectionGeometryEvent` / `SubmitCustomGeometryEvent` path. The shared mixin configuration still lists these class names and is outside this assignment's ownership.
- **Required change:** Remove those five exact obsolete entries from `gtceu.mixins.json`; keep the independent outline/block-renderer mixins and the optional `normal.embeddium` / `safemode.embeddium` entries tracked by request 15.
- **Behavior/lifecycle:** `BloomChunkGeometry` captures per-section 26.2 baked quad colors, tint/shade, material emission and vertex light, UV, overlay, and normal data; visible snapshots are submitted to the existing bloom pipeline. Section revision/world generation guards and chunk, level, and resource-reload invalidation replace the old render-task hooks and GPU-buffer cleanup.
- **Evidence/impact:** Patched MC 26.2 `SectionCompiler.compile` calls NeoForge additional section renderers with the live `ModelBlockRenderer` and region; NeoForge `AddSectionGeometryEvent` documents the worker-thread callback, and `SubmitCustomGeometryEvent` provides visible sections plus the frame collector. `build/friend-client-port-bloom-section-geometry-verify2.log` reports 718 global / 370 exact-owned diagnostics with no errors in the replacement bloom classes. Until the config entries are removed, game launch may fail while resolving the deleted mixin classes; runtime launch was not attempted per the handoff.
- **Blocks:** client startup/mixin application if the stale config entries remain; Java compilation itself is unaffected.
- **Owned implementation:** `client/bloom/BloomChunkGeometry.java`, `client/bloom/BloomRenderer.java`, and `client/bloom/BloomEventListeners.java`.

## Resolved

None.
